### 📊 Сравнение AirFlow и SSIS

Чтобы сразу понять место AirFlow в экосистеме, полезно сравнить его с SSIS по основным параметрам.

| **Параметр** | **Apache AirFlow** | **Microsoft SSIS (SQL Server Integration Services)** |
| :--- | :--- | :--- |
| **Основная концепция** | Оркестратор рабочих процессов (workflows) как код. Не выполняет преобразования напрямую, а управляет задачами. | ETL-инструмент с графическим дизайнером для проектирования потоков данных (пакетов). |
| **Модель разработки** | Конфигурация как код (Python). DAG описываются в Python-скриптах. | Drag-and-drop графический интерфейс в Visual Studio, с возможностью расширения на C#. |
| **Оркестрация vs Трансформация** | **Сильная сторона:** Оркестрация. Отлично управляет зависимостями, расписанием и мониторингом задач, которые могут выполняться где угодно (SQL, Python-скрипты, внешние системы). | **Сильная сторона:** Трансформация данных. Имеет мощный встроенный движок и богатую библиотеку компонентов для преобразований "внутри" пакета. |
| **Требуемые навыки** | Программирование на Python, понимание принципов оркестрации, работа с CLI. | Работа с графическим интерфейсом, SQL, возможно C#/.NET для кастомизации. |
| **Платформа и лицензирование** | Кроссплатформенный (Linux, macOS, Windows), открытый исходный код (Apache 2.0). | Тесно интегрирован с экосистемой Microsoft, работает на Windows. Коммерческая лицензия в составе SQL Server. |

**Вывод:** Если SSIS — это **станция с рабочими**, которая сама выполняет всю работу, то AirFlow — это **диспетчер**, который ставит задачи рабочим (которые могут находиться где угодно: в облаке, в базе данных, в скрипте) и следит, чтобы всё было сделано вовремя и в правильном порядке.

### 🏗️ Архитектура и ключевые компоненты AirFlow

AirFlow имеет модульную архитектуру, состоящую из нескольких сервисов.

```python
# Пример простого DAG (Directed Acyclic Graph) - основа любого рабочего процесса в AirFlow.
from airflow import DAG
from airflow.operators.bash import BashOperator
import pendulum

with DAG(
    dag_id="simple_etl_example",
    start_date=pendulum.datetime(2025, 8, 23, tz="UTC"),
    schedule="0 2 * * *",  # Каждый день в 02:00
    catchup=False,
) as dag:

    # Задача 1: Извлечение (Extract)
    extract = BashOperator(
        task_id="extract_data",
        bash_command="echo 'Запускаю скрипт извлечения данных...'",
    )

    # Задача 2: Преобразование (Transform)
    transform = BashOperator(
        task_id="transform_data",
        bash_command="echo 'Запускаю скрипт преобразования...'",
    )

    # Задача 3: Загрузка (Load)
    load = BashOperator(
        task_id="load_to_dwh",
        bash_command="echo 'Загружаю данные в хранилище...'",
    )

    # Определение порядка выполнения: extract -> transform -> load
    extract >> transform >> load
```

Вот как работают основные компоненты из кода выше и системы в целом:

| **Компонент / Концепция** | **Описание** | **Аналогия с SSIS** |
| :--- | :--- | :--- |
| **DAG (Directed Acyclic Graph)** | Направленный ациклический граф — это Python-скрипт, который определяет набор задач и их зависимости. Ядро концепции AirFlow. | Весь пакет SSIS (`.dtsx` файл). |
| **Оператор (Operator)** | Шаблон для выполнения одной атомарной задачи (например, `BashOperator`, `PythonOperator`, `SqlOperator`). AirFlow содержит сотни готовых операторов. | Компонент внутри потока данных SSIS (например, "Источник OLE DB", "Преобразование данных"). |
| **Задача (Task)** | Конкретный экземпляр оператора с заданными параметрами в DAG. | Конкретно настроенный компонент на холсте пакета SSIS. |
| **Планировщик (Scheduler)** | Сервис, который отслеживает все DAG, и по расписанию создает их запуски (DAG Runs) и ставит готовые задачи в очередь. | Служба агента SQL Server, запускающая пакеты по расписанию. |
| **Исполнитель (Executor)** | Механизм, определяющий *где* и *как* выполняются задачи (локально, в кластере Celery, в Kubernetes). | Движок времени выполнения SSIS, который выполняет пакет. |
| **Веб-сервер (Web Server)** | Flask-приложение с UI для мониторинга, ручного запуска, просмотра логов и управления DAG. | Нет прямого аналога. Отчеты и журналы в SQL Server Management Studio. |

### 📝 Что важно знать для собеседования

*   **Преимущества AirFlow**: Гибкость (код — это Python), хорошая масштабируемость (через Celery/Kubernetes), мощный UI для мониторинга, большое сообщество и экосистема.
*   **Недостатки / Сложности**: Более высокий порог входа (требует навыков Python и администрирования), «оркестратор, а не ETL-инструмент» (трансформации нужно писать самим), сложность первоначальной настройки.
*   **Сценарии использования**: Автоматизация ETL/ELT, оркестрация ML-пайплайнов, запуск периодических отчетов, управление любыми сложными зависимыми задачами.
*   **Лучшие практики**: Делать задачи идемпотентными (повторный запуск дает тот же результат), выносить параметры подключений и секреты из кода DAG, проектировать DAG как последовательность логических шагов.

