## **Глава 3: Задачи SSIS (SSIS Tasks)**

Эта глава — **энциклопедия задач Control Flow**. Авторы подробно разбирают каждую задачу, группируя их по функциональности, и приводят практические примеры использования. Понимание задач — ключ к проектированию эффективной ETL-логики.

---

### 📌 **Структура главы и классификация задач:**

Авторы разбивают задачи на логические группы:
1.  **Задачи для скриптования и .NET:** Script Task.
2.  **Задачи Analysis Services:** Execute DDL, Processing, Data Mining Query.
3.  **Задачи потока данных:** Data Flow Task (ядро ETL).
4.  **Задачи подготовки данных (Data Preparation Tasks):** File System, FTP, Web Service, XML, Data Profiler.
5.  **Задачи для работы с СУБД (RDBMS Tasks):** Execute SQL, Bulk Insert.
6.  **Задачи рабочего процесса (Workflow Tasks):** Execute Package, Execute Process, Send Mail, Message Queue, WMI.
7.  **Задачи администрирования SQL Server (SMO Tasks):** Transfer Database, Transfer Logins и др.

Давайте пройдёмся по самым критичным из них.

---

### **1. Общие свойства задачи (SSIS Task Common Properties)**
Перед разбором конкретных задач, авторы объясняют **общие свойства**, которые есть у любой задачи в SSIS. Их важно знать для тонкой настройки и отладки.

| Свойство | Описание | Важно для собеседования |
| :--- | :--- | :--- |
| **DelayValidation** | Если `True`, валидация свойств задачи откладывается до времени выполнения. Полезно, если, например, целевая таблица создаётся в этом же пакете. | **Да.** Показывает понимание порядка валидации. |
| **Disable** | Временно отключает задачу (как закомментировать код). | Для отладки. |
| **FailPackageOnFailure** | Если `True`, ошибка в этой задаче приведёт к падению всего пакета. | Понимание вложенных транзакций и обработки ошибок. |
| **IsolationLevel & TransactionOption** | Настройки уровня изоляции и участия в транзакции. | **Да.** Вопросы по транзакционности в SSIS. |
| **LoggingMode** | Определяет, будет ли задача логировать события. | Для администрирования. |
| **ExecValueVariable** | Имя переменной, в которую будет записан **результат выполнения** задачи (например, количество обработанных строк из Execute SQL). | **Очень важно!** Позволяет передавать результаты между задачами и строить динамические workflow. |

---

### **2. Script Task (.NET)**
- **Назначение:** Выполнение пользовательского кода (C# или VB.NET) в среде **Visual Studio Tools for Applications (VSTA)**.
- **Преимущества перед старым ActiveX Script:** IntelliSense, отладка, доступ к полной мощи .NET Framework.
- **Конфигурация (вкладка Script):**
    - **ScriptLanguage:** Выбор C# или VB.NET.
    - **ReadOnlyVariables / ReadWriteVariables:** Список переменных SSIS, доступных для чтения/записи в коде. **Ключевой момент** для обмена данными.
    - **Edit Script:** Открывает VSTA-редактор.
- **Пример в книге:** Проверка существования файла и вывод значения переменной `User::MyValue` в MessageBox.

**Для собеседования:** *"Использую Script Task, когда встроенных задач SSIS недостаточно. Например, для сложной логики валидации, работы со специальными API или парсинга нестандартных форматов данных. Всегда явно указываю переменные в ReadOnly/ReadWrite для ясности."*

---

### **3. Data Flow Task**
- **Самая важная задача.** Это **контейнер для ETL-операций**. Внутри неё происходит извлечение, преобразование и загрузка данных.
- При двойном клике открывается вкладка **Data Flow**.
- Подробно разбирается в **Главе 5**, но здесь авторы подчёркивают её роль как "сердца SSIS".

---

### **4. Задачи подготовки данных (Data Preparation Tasks)**

#### **A. File System Task**
- **Назначение:** Операции с файлами и папками (копировать, переместить, удалить, переименовать, создать, изменить атрибуты).
- **Не требует скриптов!** Полная замена FileSystemObject из VBScript.
- **Пример из книги (Archiving a File):** Задача настроена на перемещение (`Move File`) обработанного файла `MyFile.txt` из рабочей папки в папку `Archive`. Показывает, как использовать **File Connection Manager**.

#### **B. FTP Task**
- **Назначение:** Отправка и получение файлов по FTP.
- **Новое в SSIS (по сравнению с DTS):** Поддержка **пассивного режима** (решение проблем с фаерволом).
- **Пример из книги (Getting a File Using FTP):** Загрузка файла `readme.txt` с публичного FTP. Показывает настройку соединения, выбор операции (`Receive files`), использование переменной для локального пути.

#### **C. Web Service Task**
- **Назначение:** Вызов метода веб-сервиса и сохранение результата (XML) в файл или переменную.
- **Требует:** HTTP Connection Manager, указывающий на WSDL.
- **Пример из книги:** Вызов метода `GetInfoByZipCode` веб-сервиса USZIP для получения данных о почтовом индексе. Результат сохраняется в XML-файл.
- **Комбинированный пример:** Авторы показывают, как **объединить Web Service Task с XML Task и Data Flow**.
    1.  Web Service Task получает XML.
    2.  **XML Task** может проверить или преобразовать его.
    3.  **XML Source** в Data Flow считывает этот файл, используя сгенерированный XSD, и загружает данные в плоский файл или БД.

#### **D. XML Task**
- **Назначение:** Операции с XML: валидация (по XSD или DTD), преобразование (XSLT), извлечение узлов (XPATH), сравнение (Diff), слияние (Merge), применение изменений (Patch).
- **Пример из книги (Validating an XML File):** Валидация XML-файла, полученного от веб-сервиса, с использованием XSD-схемы. Показывает обработку двух сценариев: корректный файл и файл с ошибкой (`<CITYZ>` вместо `<CITY>`).

#### **E. Data Profiling Task**
- **Назначение:** **Анализ качества данных** в источнике. Позволяет обнаружить: процент NULL, распределение значений, кандидаты на ключи, функциональные зависимости и т.д.
- **Итог сохраняется** в специальный XML-файл, который просматривается в отдельном **Data Profile Viewer**.
- **Важно:** Это **инструмент анализа**, а не очистки. Помогает принять решение о том, какие преобразования нужны в Data Flow.

**Для собеседования (по подготовке данных):** *"Перед загрузкой данных из нового источника использую Data Profiling Task, чтобы понять их структуру и качество. Для автоматизации загрузки файлов применяю File System и FTP Task. Если источник предоставляет данные через API, использую Web Service Task в связке с XML Task для обработки ответа."*

---

### **5. Задачи для работы с СУБД (RDBMS Tasks)**

#### **A. Execute SQL Task**
- **"Швейцарский нож"** SSIS. Выполняет SQL-запросы или хранимые процедуры.
- **Ключевые возможности:**
    - **Параметры (Parameter Mapping):** Передача переменных SSIS в SQL-запрос (Input) и получение результатов обратно (Output).
    - **Результат (Result Set):** Запрос может вернуть:
        - **Single row** → в одну переменную (Object) или набор переменных.
        - **Full result set** → в переменную типа `Object` (затем можно прочитать через Foreach Loop или Script Task).
        - **XML** → в переменную типа `String`.
- **Пример в книге:** Запрос, возвращающий список таблиц для обработки (`Full result set`), который затем передаётся в цикл Foreach.

#### **B. Bulk Insert Task**
- **Назначение:** Самая быстрая загрузка данных из текстового файла в SQL Server (использует команду `BULK INSERT`).
- **Ограничения:** Работает **только с SQL Server** и текстовыми файлами. Минимум преобразований.
- **Альтернатива:** Data Flow Task с OLE DB Destination, который также может использовать механизм быстрой загрузки (Fast Load).

**Для собеседования:** *"Для максимально быстрой загрузки больших текстовых файлов в SQL Server использую Bulk Insert Task. Если нужны преобразования или загрузка из другого источника, применяю Data Flow Task с соответствующими компонентами."*

---

### **6. Задачи рабочего процесса (Workflow Tasks)**

#### **A. Execute Package Task**
- **Назначение:** Запуск другого пакета SSIS из текущего (создание модульных, переиспользуемых решений).
- **Можно передавать** значения переменных в дочерний пакет.

#### **B. Send Mail Task**
- **Назначение:** Отправка email-уведомлений через SMTP.
- **Типовое использование:** В обработчике события `OnError` для оповещения об ошибке в пакете.

#### **C. WMI Tasks (Data Reader & Event Watcher)**
- **Назначение:** Взаимодействие с **Windows Management Instrumentation**.
    - **WMI Data Reader:** Выполняет WQL-запрос (аналог SQL для WMI). Например, получить список процессов или информацию о железе.
    - **WMI Event Watcher:** **Ожидает** наступления события в системе (например, появление файла в папке). Пакет "засыпает" и просыпается при событии.

**Пример из книги (Polling a Directory):** WMI Event Watcher Task ожидает события создания файла в указанной папке. Как только файл появляется, пакет продолжает выполнение (например, начинает его обработку).

---

### **7. Задачи администрирования SQL Server (SMO Tasks)**
Группа задач для **управления и миграции** объектов SQL Server (баз данных, логинов, заданий, объектов). Основаны на SQL Management Objects (SMO).
- **Примеры:** Transfer Database Task, Transfer Logins Task, Transfer SQL Server Objects Task.
- **Использование:** Часто применяются в пакетах для **развёртывания** или создания резервных копий сред.

---

### 🎯 **Что важно запомнить из Главы 3:**

1.  **Script Task** — для кастомной логики на .NET. Чётко определяйте переменные для обмена.
2.  **Data Flow Task** — ядро ETL, но это всего лишь одна задача в Control Flow.
3.  **Execute SQL Task** — мощнейший инструмент. Уметь работать с **Parameter Mapping** и **Result Sets** — обязательно.
4.  **File System, FTP, Web Service** — задачи для взаимодействия с внешним миром (файлы, сеть).
5.  **XML Task** — для работы с XML (валидация, преобразование). Часто идёт в паре с Web Service.
6.  **WMI Event Watcher** — позволяет пакету **реагировать на события ОС**, а не только запускаться по расписанию.
7.  **Send Mail Task** — стандартный способ уведомлений.
8.  **Общее свойство `ExecValueVariable`** — механизм для передачи результатов выполнения задачи дальше по потоку.

---

### ❓ **Пример вопроса на собеседованнии из темы Главы 3:**

*"Вам нужно обработать все файлы `.csv`, которые появляются в папке в течение дня. Как бы вы построили пакет SSIS?"*

**Ответ (с использованием знаний из главы):**
1.  **WMI Event Watcher Task** или **цикл Foreach Loop** (о нём в Главе 4) для мониторинга папки.
2.  Внутри цикла **File System Task** для перемещения найденного файла в рабочую директорию.
3.  **Data Flow Task** для загрузки данных из CSV (Flat File Source) в БД (OLE DB Destination) с необходимыми преобразованиями.
4.  После успешной загрузки — ещё один **File System Task** для архивации/удаления файла.
5.  **Execute SQL Task** для логирования успешной обработки.
6.  Обработчик события `OnError` с **Send Mail Task** для уведомления об ошибке.

---

✅ **Итог по Главе 3:**
Это **справочник по инструментарию Control Flow**. Авторы не просто перечисляют задачи, а показывают их место в реальных ETL-процессах. Ключевой вывод: SSIS предоставляет богатый набор готовых задач, покрывающих 90% потребностей ETL, что избавляет от написания тонн скриптового кода.
