## **Глава 7: Соединение данных (Joining Data)**

Эта глава отвечает на фундаментальный вопрос: **"Как эффективно объединять данные из разных потоков или систем в SSIS?"** Авторы не просто описывают компоненты, а сравнивают разные подходы, объясняют их внутреннюю работу и дают рекомендации по выбору в зависимости от сценария.

---

### 📌 **Три основных способа соединения данных в SSIS:**

1.  **Join в источнике (Source Join):** Использовать возможности исходной СУБД (SQL `JOIN` в запросе OLE DB Source).
2.  **Merge Join Transformation:** Классическое соединение двух отсортированных потоков данных *внутри* Data Flow.
3.  **Lookup Transformation:** Сопоставление данных основного потока со справочной таблицей (де-факто стандарт для обогащения данных).

---

### **1. Сравнение SSIS и реляционного соединения**

**Ключевой тезис главы:** **Не всегда нужно делать join в SSIS.** Часто это можно и нужно делать на стороне источника данных.

| Подход | Плюсы | Минусы | Когда использовать |
| :--- | :--- | :--- | :--- |
| **Join в источнике (SQL `JOIN`)** | **Максимальная производительность.** Использует оптимизатор СУБД, индексы. Минимум данных передаётся в SSIS. | Нагружает систему-источник. Требует прямого доступа к обеим таблицам в одной СУБД. | **Всегда, когда возможно.** Источник — реляционная БД, и таблицы находятся в одной БД/на одном сервере. |
| **Merge Join (в SSIS)** | Работает полностью **в памяти** (после сортировки). Не нагружает источник. | **Оба потока должны быть отсортированы** (Sort Transformation — блокирующая операция!). Требует много памяти. | Когда данные уже отсортированы или приходят из несортируемых источников (файлы, веб-сервисы). Для сложных неэквисоединений (non-equi joins). |
| **Lookup (в SSIS)** | Гибкая работа с **кэшированием**. Можно соединять данные из разных типов источников (файл + БД). | Производительность сильно зависит от выбора режима кэширования. | **Стандартный сценарий:** Обогащение основного потока справочными данными (например, подстановка `ProductName` по `ProductID`). |

**Вывод авторов:** **Первый выбор — всегда реляционный JOIN в источнике.** SSIS-компоненты — для случаев, когда это невозможно.

---

### **2. Lookup Transformation: Детальный разбор (САМАЯ ВАЖНАЯ ЧАСТЬ)**

Lookup — самый часто используемый компонент для соединения. Глава посвящает ему основное внимание, разбирая **режимы кэширования** — ключ к пониманию его производительности.

#### **A. Режимы кэширования (Cache Mode)**

| Режим | Принцип работы | Память | Производительность | Когда использовать |
| :--- | :--- | :--- | :--- | :--- |
| **Full Cache** (Полный кэш) | **До начала** обработки основного потока **весь** справочник загружается в память. | **Высокое** (весь справочник). | **Очень высокая.** Сопоставление идёт с RAM. | Справочник **помещается в память** и **не меняется** часто. Идеальный вариант. |
| **Partial Cache** (Частичный кэш) | Кэш пуст в начале. При поступлении строки ищет значение в БД и **запоминает (кэширует)** его. Последующие совпадения берутся из кэша. | **Переменное** (хранит только найденные значения). | **Высокая,** если много повторов в данных. Ниже при низком проценте попаданий. | Большой справочник, который не помещается в память, **но данные в основном потоке имеют много повторов** (например, 100 заказов на 20 товаров). |
| **No Cache** (Без кэша) | **Для каждой строки** основного потока выполняется запрос к БД. | **Минимальное.** | **Очень низкая** (N запросов к БД). | **Крайне редко.** Только если справочник огромен, процент попаданий мизерный, а память сильно ограничена. |

**Ключевая настройка:** **Размер кэша (Cache size)** и его сброс. Можно задать лимит памяти (в %) для Partial Cache.

#### **B. Дополнительные возможности Lookup:**
- **Обработка строк без совпадений (Error Output):** Можно направить строки без найденного соответствия в отдельный поток (например, для отбрасывания или обработки ошибок).
- **Множественные выходы (Multiple Outputs):** Разделить поток на "найденные" и "ненайденные" строки.
- **Выражения (Expressions):** Свойство `SqlCommand` можно задать выражением, чтобы динамически менять запрос к справочнику.
- **Каскадные операции (Cascaded Lookups):** Последовательное выполнение нескольких Lookup-преобразований (риск потери производительности).

---

### **3. Merge Join Transformation**

- **Принцип работы:** Классический алгоритм слияния (merge) **двух предварительно отсортированных** потоков.
- **Требования:** Оба входа **должны быть отсортированы** по ключу соединения. Это достигается либо `ORDER BY` в источнике, либо **Sort Transformation** (дорогая операция!).
- **Типы соединений:** `INNER JOIN`, `LEFT OUTER JOIN`, `FULL OUTER JOIN`.
- **Когда использовать:** Когда нужно соединить **два больших, уже отсортированных потока**, или когда нужен `FULL OUTER JOIN` (чего Lookup не умеет).

**Пример из книги:** Авторы показывают, как выполнить `INNER JOIN` между данными о продажах и продуктами, предварительно отсортировав оба потока.

---

### **4. Практические примеры и сравнение**

В главе приведён подробный пример построения пакета тремя разными способами для одного сценария (объединение `Sales` и `Products`):

1.  **Реляционный JOIN в OLE DB Source:** Самый простой и быстрый. Один компонент Source с запросом `SELECT * FROM Sales s INNER JOIN Products p ON s.ProductID = p.ProductID`.
2.  **Merge Join:** Два источника (Sales и Products) → два преобразования Sort (по `ProductID`) → Merge Join. Наглядно демонстрирует **"налог" на сортировку** внутри SSIS.
3.  **Lookup:** Один источник (Sales) → Lookup (справочник — таблица Products). Демонстрирует настройку кэширования.

**Результат:** Наглядная демонстрация, что реляционный JOIN почти всегда выигрывает по простоте и производительности.

---

### **5. Cache Connection Manager и Cache Transform**

- **Cache Connection Manager (CCM):** Позволяет **сохранить результат работы Lookup** (или любого набора данных) в специальный кэш, который может быть **переиспользован в других Data Flow или даже пакетах** в рамках одного выполнения.
- **Cache Transform:** Наполняет CCM данными.
- **Сценарий использования:** Один и тот же большой справочник используется в **нескольких Lookup-преобразованиях** в рамках одного пакета. Вместо загрузки его в память несколько раз, он загружается один раз в CCM.

**Пример:** Справочник стран используется для обогащения данных о клиентах и данных о поставщиках в одном пакете. Загружаем страны в кэш один раз и переиспользуем.

---

### 🎯 **Что важно запомнить из Главы 7:**

1.  **Первый закон SSIS-оптимизации:** **Делай JOIN в источнике (Source), если можешь.** Используй мощность СУБД.
2.  **Lookup — твой основной инструмент** для обогащения данных. **Выбор режима кэширования — это 90% успеха.**
    - **Full Cache** — для маленьких/средних, статичных справочников.
    - **Partial Cache** — для больших справочников с повторяющимися значениями в основном потоке.
    - **No Cache** — почти никогда.
3.  **Merge Join** — специализированный инструмент. Помни про **обязательную сортировку** и её стоимость.
4.  **Cache Connection Manager** — для оптимизации, когда один справочник используется многократно.
5.  **Соединение данных — это операция, требующая памяти.** Планируй использование ресурсов.

---

### ❓ **Пример вопроса на собеседованнии из темы Главы 7:**

*"У вас есть основной поток в 1 млн строк заказов (`OrderID`, `ProductID`, `Qty`) и справочник товаров (`ProductID`, `Name`, `Price`) на 100 тыс. записей. Нужно добавить название и цену товара к каждому заказу. Как вы построите Data Flow и почему?"*

**Развёрнутый ответ (демонстрирующий знание главы):**
*"Я проанализирую сценарий. Справочник в 100 тыс. строк — относительно небольшой и, скорее всего, поместится в память. Основной поток большой. Поэтому мой выбор — **Lookup Transformation в режиме Full Cache**.*
1.  *Настрою OLE DB Source на выборку заказов.*
2.  *Добавлю Lookup, укажу ему на таблицу товаров. В настройках выберу **Full Cache**. Это загрузит весь справочник в память до начала обработки заказов, и дальше каждый `ProductID` будет искаться в оперативной памяти, что очень быстро.*
3.  *Настрою сопоставление по `ProductID` и выберу для возврата столбцы `Name` и `Price`.*
4.  *Настрою обработку ошибок: строки, для которых не найдётся товар (неверный `ProductID`), направлю в отдельный поток для записи в таблицу ошибок.*
* **Альтернатива — Merge Join** была бы хуже, так как потребовала бы предварительной сортировки обоих потоков (1 млн + 100 тыс. строк), что является дорогой блокирующей операцией.*
* **Join в источнике** был бы лучшим вариантом, если бы таблицы заказов и товаров находились в одной базе данных. Но в SSIS мы часто работаем с гетерогенными источниками."*

---

✅ **Итог по Главе 7:**
Эта глава даёт **системный подход к проектированию операций соединения**. Она учит не просто использовать компоненты, а **выбирать правильный инструмент на
