## **Глава 2: Инструменты SSIS**

Эта глава посвящена **инструментам разработки и администрирования SSIS**. Авторы подробно разбирают, как использовать Import/Export Wizard, SQL Server Data Tools (SSDT) и SQL Server Management Studio (SSMS) для работы с пакетами.

---

### 📌 **Ключевые разделы главы:**

---

#### **1. Import and Export Wizard (Мастер импорта/экспорта)**
- **Назначение:** Быстрый перенос данных между источниками (SQL Server, Oracle, Excel, текстовые файлы и т.д.).
- **Как запустить:**
  1. Из SSMS: правой кнопкой по БД → Tasks → Import/Export Data.
  2. Из SSDT: Project → SSIS Import and Export Wizard.
  3. Из меню Пуск: SQL Server → Import and Export Data.
  4. Командная строка: `dtswizard.exe`.
- **Что умеет:**
  - Копировать таблицы/представления.
  - Выполнять SQL-запрос для выборки данных.
  - Сохранять логику в пакет `.dtsx` (можно потом доработать в SSDT).
  - Настраивать сопоставление столбцов, создавать таблицы в назначении.
- **Ограничения:** Минимальные преобразования данных. Пакеты, созданные мастером, часто не соответствуют best practices.

**Для собеседования:**  
*"Использую Import/Export Wizard для быстрых одноразовых переносов или как основу для будущего пакета. Для сложной ETL-логики всегда разрабатываю пакет вручную в SSDT."*

---

#### **2. SQL Server Data Tools (SSDT) — основная среда разработки**

##### **A. Общее описание**
- SSDT — это **специализированная версия Visual Studio** для разработки под SQL Server (SSIS, SSAS, SSRS).
- Проекты и решения (Solutions/Projects) — как в обычной Visual Studio.
- **Проект SSIS** содержит пакеты (.dtsx), общие диспетчеры соединений (.conmgr), параметры проекта (.params).

##### **B. Окна SSDT**
1. **Solution Explorer** — дерево проекта (пакеты, Connection Managers, Miscellaneous файлы).
2. **Toolbox (Панель элементов)** — задачи для Control Flow, компоненты для Data Flow. Группируется по категориям (Favorites, Common, Containers, Other Tasks).
3. **Properties Window (Окно свойств)** — свойства выбранного объекта (задачи, соединения, пакета). **F4** — горячая клавиша.
4. **Variables Window (Окно переменных)** — список переменных пакета. Можно показывать системные переменные (PackageName, StartTime и т.д.).

##### **C. Вкладки конструктора пакета (Package Designer Tabs)**
- **Control Flow** — проектирование потока задач.
- **Data Flow** — проектирование потока данных (открывается при двойном клике на Data Flow Task).
- **Parameters** — параметры пакета (вошли в SSIS 2012).
- **Event Handlers** — обработчики событий (OnError, OnPostExecute и др.).
- **Package Explorer** — иерархическое представление всех элементов пакета.

##### **D. Работа в Control Flow**
- **Задачи (Tasks)** — перетаскиваются из Toolbox.
- **Precedence Constraints (Ограничения предшествования)** — соединяют задачи. Бывают:
  - **Success** (зелёная стрелка) — выполнится, если предыдущая задача успешна.
  - **Failure** (красная стрелка) — выполнится, если предыдущая задача завершилась ошибкой.
  - **Completion** (синяя стрелка) — выполнится в любом случае.
- **Выражения (Expressions)** в Constraints — дополнительная логика (например, `@[User::FileExists] == TRUE`).
- **Группировка задач (Grouping)** — визуальное объединение (не путать с **контейнерами**!).
- **Аннотации (Annotations)** — комментарии в пакете (лучшая практика — добавлять описание и историю версий).

##### **E. Connection Managers (Диспетчеры соединений)**
- Централизованное управление подключениями (к БД, файлам, FTP, HTTP и т.д.).
- Бывают **проектные** (видны во всех пакетах проекта) и **пакетные** (только в одном пакете).
- Создаются в нижней части конструктора (вкладка Connection Managers).

---

#### **3. Создание первого пакета (практический пример в главе)**
Авторы показывают создание простого пакета с двумя задачами **Execute Process Task**:
1. Запуск `notepad.exe`.
2. Запуск `calc.exe`.
Связаны ограничением **Success** — калькулятор откроется только после закрытия блокнота.

**Важный момент:** Пакеты с интерактивными окнами (notepad, calc) не для продакшена — только для демонстрации логики.

---

#### **4. SQL Server Management Studio (SSMS) — инструмент администрирования**
- **Не для разработки пакетов!** Только для управления, запуска, настройки безопасности.
- Подключение к **Database Engine** для управления заданиями агента.
- Подключение к **Integration Services** (старая служба, для пакетов, хранящихся в MSDB или файловой системе — **Package Deployment Model**).
- В SSIS 2012+ основная модель — **Project Deployment Model** с каталогом **SSISDB**, который управляется через SSMS (но разработка — в SSDT).

---

### 🎯 **Что важно запомнить из Главы 2:**

1. **Import/Export Wizard** — для быстрых переносов, но не для production ETL.
2. **SSDT** — основная среда разработки. Ключевые окна: Solution Explorer, Toolbox, Properties, Variables.
3. **Вкладки пакета**: Control Flow, Data Flow, Parameters, Event Handlers, Package Explorer.
4. **Precedence Constraints** — сердце Control Flow. Умеют работать с выражениями.
5. **Connection Managers** — единая точка управления подключениями.
6. **Группировка != Контейнеры** — группы только визуальные, контейнеры имеют логическую область.
7. **SSMS** — для администрирования, а не разработки.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 2:**

*"Какие инструменты вы используете для разработки и администрирования пакетов SSIS?"*

**Ответ:**
- **Разработка:** SQL Server Data Tools (SSDT) — создаю проекты, проектирую Control Flow и Data Flow, настраиваю параметры и переменные.
- **Администрирование:** SQL Server Management Studio (SSMS) — запускаю пакеты, настраиваю безопасность каталога SSISDB, мониторю выполнение, работаю с SQL Agent для планирования.
- **Быстрый перенос:** Import/Export Wizard — если нужно быстро скопировать данные и сохранить логику в пакет для последующей доработки.

---

### 🔧 **Практический совет из главы:**
Включите **подписи для Precedence Constraints** (зелёная/красная/синяя стрелки), если вы или коллеги плохо различаете цвета.  
*Настройка:* Tools → Options → Business Intelligence Designers → Integration Services Designers → "Show precedence constraint labels".

---

✅ **Итог по Главе 2:**  
Глава даёт чёткое понимание **когда и какой инструмент использовать**. Ключевое — **разделение ролей**: SSDT для разработчика, SSMS для администратора. Также важно понять разницу между **Package Deployment Model** (старая модель, управляется через службу SSIS) и **Project Deployment Model** (новая модель, каталог SSISDB). Это станет важнее в последующих главах.

---
