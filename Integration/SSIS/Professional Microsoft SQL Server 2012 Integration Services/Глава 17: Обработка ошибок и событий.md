**Глава 17: Error and Event Handling** — это руководство по созданию **надёжных, самодиагностируемых и самоисцеляющихся ETL-процессов**, которые не просто падают при первой же ошибке, а грамотно на них реагируют.

---

## **Глава 17: Обработка ошибок и событий**

Эта глава — о том, как **предвидеть неожиданное** и встроить в пакеты механизмы для graceful degradation (штатной обработки сбоев), уведомлений и восстановления. Это ключ к спокойному сну по ночам, когда ваши ETL-пакеты работают в production.

---

### 📌 **Ключевые темы главы:**

1.  **Использование ограничений предшествования (Precedence Constraints).**
2.  **Обработчики событий (Event Handlers).**
3.  **Точки останова (Breakpoints) для отладки.**
4.  **Выходы ошибок (Error Rows) в Data Flow.**
5.  **Логирование (Logging).**

---

### **1. Использование ограничений предшествования (Using Precedence Constraints)**

Мы уже знаем основы: зеленый (Success), красный (Failure), синий (Completion). Здесь авторы углубляются в **расширенные сценарии**.

#### **A. Основы ограничений предшествования (Precedence Constraint Basics)**
- Связывают задачи в Control Flow.
- Определяют **условие перехода**: успех, неудача или завершение предыдущей задачи.

#### **B. Расширенные ограничения и выражения (Advanced Precedence Constraints and Expressions)**
- **Самая мощная фича:** Возможность добавить **выражение (Expression)** к ограничению.
- **Evaluation Operation (Операция оценки):**
    - **Constraint:** Только результат выполнения задачи (Success/Failure/Completion).
    - **Expression:** Только результат вычисления выражения (логическое `TRUE`/`FALSE`).
    - **Expression and Constraint:** **И** результат задачи, **И** выражение должны быть истинны (логическое И). *Пример: "Задача выполнена успешно И (&&) сегодня понедельник".*
    - **Expression or Constraint:** **ИЛИ** результат задачи, **ИЛИ** выражение должны быть истинны (логическое ИЛИ). *Пример: "Задача выполнена с ошибкой ИЛИ (||) файл для обработки не найден".*
- **Практические примеры:**
    1.  **Запуск по расписанию внутри пакета:** Задача "Проверить день недели" (Script Task) → ограничение с выражением `@[User::IsBusinessDay] == TRUE` → задача "Загрузить данные".
    2.  **Обработка альтернативного сценария:** Если File System Task не нашла файл (Failure), И (`Expression and Constraint`) переменная `@[User::AllowEmptyExtract] == TRUE`, то перейти к задаче "Отправить предупреждение", а не "Завершить с ошибкой".
    3.  **Условное выполнение cleanup:** После основной задачи запустить "Очистить временные файлы" всегда (Completion), но только если (Expression) `@[User::DebugMode] == FALSE`.

**Для собеседования:** *"Я использую Expression в Precedence Constraints для реализации сложной бизнес-логики потока. Например, чтобы пакет загружал данные только в рабочие дни или чтобы при отсутствии файла он переходил не в ошибку, а в режим отправки уведомления и graceful shutdown."*

---

### **2. Обработчики событий (Event Handlers)**

Это **отдельные рабочие процессы (workflows)**, которые выполняются в ответ на определённые **события**, происходящие с пакетом, контейнером или задачей.

#### **A. События (Events)**
Обработчики могут реагировать на множество событий. Наиболее важные:

| Событие | Когда возникает | Типичное использование |
| :--- | :--- | :--- |
| **OnError** | При возникновении **любой ошибки** в области видимости обработчика. | **Отправка email-оповещения**, запись деталей ошибки в специальную таблицу, попытка корректирующих действий. |
| **OnWarning** | При возникновении предупреждения. | Логирование предупреждений, которые не являются критическими. |
| **OnPreExecute** | Непосредственно **перед** выполнением объекта (пакета, задачи). | Инициализация переменных, логирование начала выполнения. |
| **OnPostExecute** | Непосредственно **после** успешного выполнения объекта. | Логирование завершения, обновление переменных состояния (например, `LastSuccessDate`). |
| **OnTaskFailed** | При **сбое конкретной задачи**. Более специфично, чем `OnError`. | Специфичные действия при падении определённой задачи (например, повторный запуск задачи-загрузчика). |
| **OnVariableValueChanged** | При изменении значения переменной. | Отслеживание критических изменений состояния. |

#### **B. Использование обработчиков событий (Using Event Handlers)**
- У каждого объекта (Package, Container, Task) есть своя **вкладка Event Handlers** в дизайнере SSDT.
- На этой вкладке вы создаёте **новый workflow** (из задач и контейнеров), который выполнится, когда событие произойдёт.
- **Область видимости (Scope):** Обработчик, созданный на уровне пакета, будет ловить события **со всего пакета**. Обработчик на уровне задачи — только события этой задачи.

#### **C. Пример: Глобальный перехват ошибок (Global Error Handler)**
1.  На вкладке **Event Handlers** пакета создаём обработчик для события **OnError**.
2.  Внутри этого обработчика размещаем:
    - **Script Task** или **Execute SQL Task** для записи деталей ошибки (код, описание, имя задачи, стек вызова) в таблицу логов. Получить детали ошибки можно из **системных переменных** (`ErrorCode`, `ErrorDescription`, `SourceName`), которые доступны в обработчике.
    - **Send Mail Task** для отправки алерта администратору.
3.  **Результат:** Любая ошибка в любом месте пакета будет залогирована, и администратор получит уведомление. Пакет при этом может продолжить работу или завершиться, в зависимости от настроек.

#### **D. Наследование обработчиков событий (Event Handler Inheritance)**
- Если обработчик события не определён на уровне задачи, SSIS ищет его на уровне **родительского контейнера**, затем на уровне **пакета**.
- Это позволяет задать **глобальную политику обработки ошибок** на уровне пакета и **переопределить** её для конкретных критичных задач.

---

### **3. Точки останова (Breakpoints)**
- Инструмент **отладки**, а не обработки ошибок в production.
- Позволяют приостановить выполнение пакета в SSDT и посмотреть значения переменных, состояние задач.
- Можно ставить на событиях (например, `OnPreExecute`, `OnPostExecute`, `OnError`) для отладки сложных сценариев.

---

### **4. Выходы ошибок (Error Rows) в Data Flow**
Подробно рассматривалось в главах 5 и 14, но здесь акцент на **стратегии**.
- **Redirect Row** — единственная production-ready опция.
- **Создание надежного механизма обработки:**
    1.  Настроить `Redirect Row` на ключевых источниках и преобразованиях.
    2.  Направить "плохие" строки в **скриптовый компонент (Script Component)**, который обогатит их метаданными об ошибке (ErrorCode, ErrorColumn из `ComponentMetaData`).
    3.  Записать эти строки в **специальную таблицу ошибок (ErrorLog)**. Структура: сами данные, метаданные ошибки (`ErrorCode`, `ErrorColumn`, `ErrorDescription`), технические метки (`PackageName`, `ExecutionID`, `ErrorTime`).
- **Преимущество:** Данные не теряются. Их можно проанализировать, исправить и повторно загрузить.

---

### **5. Логирование (Logging)**
Механизм, встроенный в SSIS, для записи событий выполнения пакета во внешние хранилища.

#### **A. Поставщики логирования (Logging Providers)**
SSIS может писать логи в:
- **SQL Server** (таблица `sysssislog` в выбранной БД) — **самый распространённый**.
- Текстовый файл.
- **SQL Server Profiler** (для реального времени).
- Windows Event Log.
- XML-файл.

#### **B. События для логирования (Log Events)**
Можно выбрать, какие именно события логировать (чтобы не засорять логи). Ключевые события:
- **OnError, OnWarning**
- **OnPreExecute, OnPostExecute**
- **OnTaskFailed**
- **OnVariableValueChanged**
- **PipelineComponentTime** (для анализа производительности Data Flow!)

#### **C. Логирование каталога (Catalog Logging)** — новое в SSIS 2012+
- При использовании **Project Deployment Model** и каталога **SSISDB**, логирование включается **автоматически**.
- Логи хранятся во внутренних таблицах SSISDB (`catalog.operation_messages`, `catalog.executions` и др.).
- Есть **встроенные отчёты** в SSMS для их просмотра.
- Это наиболее удобный и мощный способ логирования в современных версиях.

---

### 🎯 **Что важно запомнить из Главы 17:**

1.  **Precedence Constraints + Expressions = мощный инструмент управления потоком.** Позволяет реализовать сложную бизнес-логику выполнения.
2.  **Event Handlers — основа надёжного production-пакета.** Всегда создавайте как минимум **глобальный OnError handler** для логирования и уведомлений.
3.  **Error Output в Data Flow всегда перенаправляйте (Redirect Row).** "Плохие" данные должны не ломать процесс, а попадать в карантин для разбора.
4.  **Логирование — не опция, а необходимость.** Настройте его для ключевых событий. В SSIS 2012+ используйте возможности каталога SSISDB.
5.  **Используйте наследование обработчиков:** задайте общие правила на уровне пакета, переопределяйте там, где нужно особое поведение.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 17:**

*"Опишите, как вы организуете обработку ошибок и уведомления в production-пакетах SSIS."*

**Идеальный ответ (охватывающий все аспекты главы):**
*"Я строю многоуровневую систему обработки ошибок:*
1.  **Уровень Data Flow (ошибки данных):** Во всех критичных источниках и преобразованиях настраиваю **Error Output на Redirect Row**. "Плохие" строки направляю в специальную таблицу `ETL_ErrorLog` с полными метаданными (ошибка, строка, пакет, время). Это гарантирует, что процесс не упадёт из-за битых данных.
2.  **Уровень Control Flow (ошибки выполнения):** Использую **обработчики событий (Event Handlers)**.
    *   **OnError на уровне пакета:** В нём размещаю Execute SQL Task для записи в таблицу `ETL_ExecutionLog` (факт ошибки, код, задача) и **Send Mail Task** для срочного оповещения команды поддержки.
    *   **OnTaskFailed для критичных задач:** Например, для задачи загрузки основного факта могу добавить обработчик, который попытается перезапустить её или выполнить компенсирующее действие.
3.  **Уровень управления потоком (Precedence Constraints):** Использую **выражения в ограничениях**, чтобы реализовать условную логику. Например, если задача проверки файла завершилась с `Failure` (файл не найден), но это разрешено (`@[User::AllowEmptyFile] == true`), то пакет идёт не по пути ошибки, а по пути отправки предупреждения и graceful completion.
4.  **Логирование (Logging):** Обязательно настраиваю логирование в **SQL Server** (или использую встроенное логирование каталога SSISDB) для событий `OnError`, `OnWarning`, `OnPreExecute`, `OnPostExecute`. Это даёт полную историю для аудита и отладки.
*Такой подход делает пакеты устойчивыми к сбоям и позволяет оперативно реагировать на любые проблемы."*

---

✅ **Итог по Главе 17:**
Эта глава превращает SSIS из инструмента для «счастливого пути» (happy path) в **инструмент для построения промышленных, отказоустойчивых процессов**. Она учит думать не только о том, *что должно произойти*, но и о том, *что может пойти не так, и как на это грамотно отреагировать*. Это важнейший навык для любого, кто разрабатывает ETL для production.
