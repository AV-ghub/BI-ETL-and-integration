Эта глава для тех, кому стандартных компонентов уже недостаточно. Она учит создавать **собственные, переиспользуемые компоненты Data Flow** (источники, преобразования, назначения) на C#/.NET, погружаясь в объектную модель SSIS.

---

## **Глава 18: Программирование и расширение SSIS**

Эта глава — **практическое руководство по разработке собственных компонентов SSIS с нуля**. Авторы показывают весь путь: от создания класса, реализующего интерфейсы SSIS, до установки, отладки и использования компонента в пакетах.

---

### 📌 **Структура главы:**

1.  **Образцы компонентов (The Sample Components):** Что мы будем создавать.
2.  **Методы конвейерных компонентов (Pipeline Component Methods):** Жизненный цикл и ключевые точки расширения.
3.  **Построение компонентов (Building the Components):** Пошаговое создание источника, преобразования и назначения.
4.  **Использование компонентов (Using the Components):** Установка, отладка, применение в пакетах.

---

### **1. Образцы компонентов (The Sample Components)**

Авторы ведут читателя через создание **трёх связанных пользовательских компонентов**, имитирующих реальную задачу:
1.  **Компонент 1: Адаптер источника (Source Adapter)** — `Custom File Source`. Читает данные из кастомного текстового файла со сложной структурой (например, с заголовком и деталями в одной строке), который не может прочитать стандартный Flat File Source.
2.  **Компонент 2: Преобразование (Transformation)** — `Custom Transformation`. Выполняет специализированную бизнес-логику (например, сложный парсинг или расчёт), которую нельзя сделать штатными средствами.
3.  **Компонент 3: Адаптер назначения (Destination Adapter)** — `Custom Destination`. Записывает данные в специализированное хранилище или формат (например, в веб-сервис или NoSQL базу).

**Цель:** Показать полный цикл и различия в реализации каждого типа.

---

### **2. Методы конвейерных компонентов (Pipeline Component Methods)**

Чтобы создать компонент Data Flow, нужно создать класс, унаследованный от **`PipelineComponent`** (пространство имён `Microsoft.SqlServer.Dts.Pipeline`), и переопределить ключевые **виртуальные методы**. Эти методы вызываются движком SSIS в разные моменты жизненного цикла.

#### **A. Функциональность времени разработки (Design-Time Functionality)**
Методы, вызываемые **в SSDT при конфигурации компонента** на поверхности Data Flow.

| Метод | Когда вызывается | Что делает разработчик |
| :--- | :--- | :--- |
| **ProvideComponentProperties()** | **Первый вызов,** когда компонент добавляется в Data Flow. | Инициализация компонента: создание входов/выходов, добавление пользовательских свойств (например, путь к файлу), установка типа компонента (Source/Transformation/Destination). |
| **ReinitializeMetaData()** | При необходимости повторной валидации (например, после изменения свойств). | Сброс и перестроение метаданных (списка выходных колонок). Критически важен для источников. |
| **Validate()** | Постоянно, для проверки корректности конфигурации. | Проверяет, все ли обязательные свойства заданы (например, путь к файлу). Возвращает `DTSValidationStatus.VS_ISVALID` или код ошибки. |
| **SetComponentProperty()** <br> **SetOutputColumnProperty()** | Когда пользователь изменяет свойство в редакторе. | Реагирует на изменения свойств (например, при смене пути к файлу перечитывает метаданные). |

#### **B. Функциональность времени выполнения (Runtime)**
Методы, вызываемые **при выполнении пакета**, когда данные реально движутся.

| Метод | Когда вызывается | Что делает разработчик |
| :--- | :--- | :--- |
| **PreExecute()** | **Один раз,** перед началом обработки первой строки. Идеальное место для *дорогих* операций. | Открытие файлов, установка соединений с БД, загрузка справочников в память — всё, что нужно подготовить один раз для всего потока. |
| **ProcessInput()** (для преобразований и назначений) | **Для каждого буфера входящих данных.** | Основная рабочая лошадка для Transformations/Destinations. Здесь получают входящие буферы и обрабатывают их (преобразуют или записывают). |
| **PrimeOutput()** (для источников) | **Вызывается для источников,** чтобы начать выдачу данных. | Основной метод для Sources. Здесь компонент создаёт буферы, заполняет их данными из внешнего источника и помещает в выходы. |
| **PostExecute()** | **Один раз,** после обработки всех данных. | Закрытие файлов, соединений, освобождение ресурсов. |

#### **C. Время соединения (Connection Time)**
- Компоненты могут использовать **Connection Managers** пакета.
- Доступ к ним осуществляется через **`ComponentMetaData.RuntimeConnectionCollection`**.

---

### **3. Построение компонентов (Building the Components)**

Авторы подробно, с кодом, показывают создание каждого компонента. Давайте выделим **ключевые шаги и различия**.

#### **A. Подготовка (Preparation)**
1.  Создание **Class Library** проекта в Visual Studio.
2.  Добавление ссылок на основные сборки SSIS:
    - `Microsoft.SqlServer.ManagedDTS.dll` (общее управление)
    - `Microsoft.SqlServer.PipelineHost.dll` (ядро конвейера)
    - `Microsoft.SqlServer.DTSPipelineWrap.dll` (COM-интероп)
3.  Создание класса, наследующего от **`PipelineComponent`**.

#### **B. Построение компонента-источника (Building the Source Component)**
- **Основная задача:** Прочитать данные из внешней системы и **создать выходные буферы**.
- **Ключевые методы:**
    - `ProvideComponentProperties()`: Создать **один или несколько выходов** (например, `Output` и `ErrorOutput`). Добавить пользовательские свойства (Custom Properties) для конфигурации (например, `ConnectionType`, `FileName`).
    - `ReinitializeMetaData()` / `Validate()`: На основе конфигурации (путь к файлу) **определить структуру выходных данных** (имена и типы колонок) и добавить их к выходу через `OutputColumnCollection.Add()`.
    - `PrimeOutput()`:
        1.  Получить `PipelineBuffer` для каждого выхода через `BufferManager.CreateBuffer()`.
        2.  В цикле читать данные из внешнего источника (файла).
        3.  Для каждой прочитанной строки:
            - Заполнить буфер: `buffer.SetString(colIndex, value)`, `buffer.SetInt32(...)`.
            - Добавить строку в буфер: `buffer.AddRow()`.
        4.  После окончания данных пометить буфер как завершённый: `buffer.SetEndOfRowset()`.

**Сложность:** Необходимо точно сопоставить типы данных .NET с типами данных SSIS (`DT_STR`, `DT_I4` и т.д.).

#### **C. Построение компонента-преобразования (Building the Transformation Component)**
- **Основная задача:** Получить входящие данные, преобразовать их и отправить дальше.
- **Ключевые методы:**
    - `ProvideComponentProperties()`: Создать **как минимум один вход** (`Input`) и **один или несколько выходов** (`Output`, `ErrorOutput`).
    - `ProcessInput()`: Основной метод. Получает `PipelineBuffer` с входящими данными.
        1.  Цикл по строкам буфера: `while (buffer.NextRow())`.
        2.  Для каждой строки: прочитать значения из буфера (`buffer.GetString(colId)`), выполнить преобразование.
        3.  Найти **буфер соответствующего выхода** (через `FindColumnByLineageID` и `BufferManager`).
        4.  Записать преобразованные значения в выходной буфер (`buffer.SetString(...)`) и добавить строку (`buffer.AddRow()`).
- **Важно:** Для **синхронных** преобразований (которые не меняют количество строк) можно использовать **один и тот же буфер**, перенаправляя строки на разные выходы через `DirectRowToOutput()`.

#### **D. Построение адаптера назначения (Building the Destination Adapter)**
- **Основная задача:** Получить входящие данные и записать их во внешнюю систему.
- **Ключевые методы:**
    - `ProvideComponentProperties()`: Создать **один вход** (`Input`). Обычно нет выходов.
    - `PreExecute()`: Идеальное место для открытия соединения с целевой системой (файлом, БД, API).
    - `ProcessInput()`: Получает входящие буферы и записывает данные из них во внешнюю систему (например, формирует batch-запрос для вставки в БД).
    - `PostExecute()`: Закрыть соединение, выполнить финализирующие операции (например, `COMMIT` транзакции).

---

### **4. Использование компонентов (Using the Components)**

#### **A. Установка компонентов (Installing the Components)**
1.  **Собрать проект** в **DLL**.
2.  **Зарегистрировать DLL в GAC (Global Assembly Cache)** с помощью `gacutil.exe` или развернуть в папку, где SSIS будет её искать.
3.  **Добавить компонент в Toolbox SSDT:** Правой кнопкой в Toolbox → Choose Items → вкладка "SSIS Data Flow Items" → Обзор к вашей DLL. После этого компонент появится в Toolbox.

#### **B. Отладка компонентов (Debugging Components)**
- **Привязка к процессу (Attach to Process):** Самый частый способ.
    1.  Установить **точки останова** в коде компонента.
    2.  В Visual Studio: Debug → Attach to Process.
    3.  Выбрать процесс:
        - **Для отладки в SSDT:** `devenv.exe` (Visual Studio).
        - **Для отладки при выполнении через `dtexec`:** `dtexec.exe`.
        - **Для отладки в SQL Agent:** `SQLAGENT.EXE` или процесс, в котором запущен шаг задания.
- **Условная компиляция:** Использовать `System.Diagnostics.Debugger.Launch()` в коде компонента, чтобы запустить отладчик при выполнении.

---

### **5. Обновление до SQL Server 2012 (Upgrading to SQL Server 2012)**
- Объектная модель SSIS **в основном сохранила совместимость**.
- Компоненты, созданные для SSIS 2005/2008, скорее всего, заработают в 2012.
- Однако, для использования всех новых возможностей (например, связи с каталогом SSISDB) может потребоваться обновление кода.

---

### 🎯 **Что важно запомнить из Главы 18:**

1.  **Создание кастомных компонентов — это высший пилотаж.** Это нужно, когда штатные компоненты не справляются со специфичным источником, преобразованием или назначением.
2.  **Ключевой класс — `PipelineComponent`.** Все компоненты наследуются от него.
3.  **Чёткое разделение методов Design-Time и Runtime.** `ProvideComponentProperties`, `Validate` — для настройки в SSDT. `PreExecute`, `PrimeOutput`/`ProcessInput`, `PostExecute` — для работы с данными при запуске.
4.  **Источник (Source)** создаёт буферы и заполняет их (`PrimeOutput`).
5.  **Преобразование (Transformation)** получает и модифицирует буферы (`ProcessInput`).
6.  **Назначение (Destination)** получает буферы и записывает их (`ProcessInput`).
7.  **Отладка требует привязки к процессу** (SSDT, dtexec, SQL Agent).
8.  **Установка:** Сборка → Регистрация (GAC) → Добавление в Toolbox SSDT.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 18:**

*"В каких случаях вы бы решили создать собственный компонент SSIS вместо использования Script Component или стандартных преобразований?"*

**Развёрнутый ответ (показывающий понимание затрат и выгод):**
*"Решение о создании кастомного компонента я принимаю на основе трёх критериев: **сложность логики, необходимость переиспользования и требования к производительности**.*
1.  **Сложность и специфика доступа к данным:** Если мне нужно читать данные из **специфичного, не поддерживаемого стандартно источника** (например, проприетарный бинарный формат, специализированное API с пагинацией) или писать в такое же назначение, Script Component как источник/назначение может не справиться или код будет очень громоздким. В этом случае кастомный компонент — единственный чистый вариант.
2.  **Массовое переиспользование в команде/проекте:** Если одна и та же сложная логика преобразования (например, парсинг определённого формата адреса) нужна в **десятках разных пакетов**, то выносить её в Script Component каждый раз — это копипаста и кошмар поддержки. Кастомный компонент, установленный в Toolbox у всех разработчиков, обеспечивает **единообразие и централизованное обновление**.
3.  **Критическая производительность:** Script Component, особенно в асинхронном режиме, может быть не самым быстрым. Для операций, которые должны обрабатывать **миллионы строк с максимальной скоростью**, тщательно оптимизированный кастомный компонент, написанный на чистом C# с минимумом накладных расходов, может дать выигрыш в производительности.
*Однако, создание и поддержка кастомных компонентов — это **большие накладные расходы** (отладка, установка, версионирование). Поэтому для одноразовой задачи или простой логики я всегда предпочту **Script Component**. Кастомные компоненты — это инструмент для **архитектурных решений**, а не для повседневных скриптов."*

---

✅ **Итог по Главе 18:**
Эта глава открывает дверь в мир **расширения платформы SSIS под свои нужды**. Она показывает, что SSIS — это не закрытая «коробка», а **открытая платформа с богатой объектной моделью**. Владение этими техниками позволяет решать уникальные задачи бизнеса, создавать конкурентные преимущества и строить высокопроизводительные ETL-фреймворки. Это уровень Senior/Lead ETL-разработчика или архитектора.
