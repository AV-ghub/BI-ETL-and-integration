## **SSIS 005 — Глава 1: Добро пожаловать в SQL Server Integration Services**

Эта глава — **введение в мир SSIS**. Она объясняет, что такое SSIS, зачем он нужен, как устроен и чем отличается от старого DTS. Авторы дают обзор архитектуры и ключевых компонентов.

---

### 📌 **Ключевые моменты главы:**

---

#### **1. Исторический контекст: от DTS к SSIS**
- **DTS (Data Transformation Services)** — появился в SQL Server 7.0, был простым инструментом для переноса данных.
- **SSIS (SQL Server Integration Services)** — полностью переписанный продукт, появившийся в SQL Server 2005.
- **Почему SSIS лучше DTS**:
  - Отделение **Control Flow** (поток задач) от **Data Flow** (поток данных).
  - Богатый набор готовых компонентов (не нужно писать скрипты для базовых операций).
  - Лучшая производительность, масштабируемость, управляемость.
  - Интеграция с Visual Studio (SQL Server Data Tools).

**Для собеседования:**  
Знание эволюции показывает понимание платформы. Можно сказать: *"SSIS — это не просто новый DTS, это принципиально другая ETL-платформа с разделением потоков управления и данных."*

---

#### **2. Что нового в SSIS 2012**
- Улучшенная **управляемость и развертывание** (Project Deployment Model, каталог SSISDB).
- **Data Quality Services (DQS)** — встроенная очистка данных.
- **Улучшенные инструменты разработки** (SQL Server Data Tools).
- Поддержка **PowerShell, улучшенное логирование**.

**Для собеседования:**  
Важно понимать, что SSIS 2012 — это переход от Package Deployment Model к Project Deployment Model, появление каталога SSISDB.

---

#### **3. Инструменты разработки**
- **Import/Export Wizard** — быстрый перенос данных, можно сохранить как пакет SSIS.
- **SQL Server Data Tools (SSDT)** — основная среда разработки (на базе Visual Studio).
- **Management Studio** — для администрирования, запуска, мониторинга пакетов.

**Для собеседования:**  
Разница между SSDT (разработка) и SSMS (администрирование).

---

#### **4. Архитектура SSIS (самая важная часть главы!)**

##### **A. Пакет (Package)**
- Это **исполняемая программа** в SSIS.
- Файл с расширением `.dtsx` (XML-формат).
- Содержит Control Flow, Data Flow, переменные, параметры, обработчики событий.

##### **B. Control Flow (Поток управления)**
- **Задачи (Tasks)** — единицы работы (например, Execute SQL, Data Flow, Script Task).
- **Контейнеры (Containers)** — группируют задачи (Sequence, For Loop, Foreach Loop).
- **Ограничения предшествования (Precedence Constraints)** — связывают задачи, определяют порядок выполнения (Success, Failure, Completion + выражения).

**Пример:**  
Задача 1 → (при успехе) → Задача 2  
Задача 1 → (при ошибке) → Задача "Отправить уведомление"

##### **C. Data Flow (Поток данных)**
- **Data Flow Task** — специальная задача, внутри которой происходит ETL.
- **Источники (Sources)** — откуда берем данные (OLE DB, Flat File, XML).
- **Преобразования (Transformations)** — как меняем данные (Lookup, Derived Column, Aggregate).
- **Назначения (Destinations)** — куда пишем данные (OLE DB, Flat File).

**Важно:**  
- Control Flow — это **последовательность задач**.
- Data Flow — это **поток данных в памяти** (высокая производительность).

##### **D. Переменные и параметры**
- **Переменные (Variables)** — хранят значения внутри пакета, могут быть разной области видимости (пакет, контейнер, задача).
- **Параметры (Parameters)** — похожи на переменные, но могут задаваться извне (при запуске пакета). Появились в SSIS 2012, заменяют конфигурации.

##### **E. Обработка ошибок и логирование**
- **Event Handlers** — реагируют на события (OnError, OnWarning и т.д.).
- **Логирование (Logging)** — запись событий в SQL Server, файл, Event Log.

---

#### **5. Редакции SQL Server и возможности SSIS**
- **Express Edition** — только Import/Export Wizard.
- **Standard Edition** — почти все возможности SSIS, кроме некоторых продвинутых преобразований.
- **Enterprise Edition** — все возможности, включая:
  - **Fuzzy Lookup / Fuzzy Grouping**
  - **DQS Cleansing**
  - **CDC (Change Data Capture)**
  - **Analysis Services задачи**

**Для собеседования:**  
Если спрашивают про Fuzzy Lookup — это Enterprise Edition.

---

### 🎯 **Что важно запомнить из Главы 1:**

1. **SSIS ≠ DTS** — это новая архитектура с разделением Control Flow и Data Flow.
2. **Control Flow** — это задачи + контейнеры + precedence constraints.
3. **Data Flow** — это источник → преобразования → назначение (всё в памяти).
4. **Пакет** — исполняемая единица, XML-файл `.dtsx`.
5. **Переменные vs Параметры** — параметры задаются извне.
6. **Enterprise Edition** даёт расширенные функции (Fuzzy, DQS, CDC).

---

### ❓ **Пример вопроса на собеседовании из темы Главы 1:**

*"Объясните разницу между Control Flow и Data Flow в SSIS."*

**Ответ:**
- **Control Flow** — определяет порядок выполнения задач (например, сначала очистить таблицу, затем загрузить данные, затем отправить отчет). Работает с задачами: Execute SQL, Script Task, Data Flow Task и др.
- **Data Flow** — отвечает за перемещение и преобразование данных. Это ядро ETL: источник → преобразования (Lookup, Derived Column) → назначение. Data Flow выполняется внутри задачи Data Flow Task в Control Flow.

---

### 📚 **Что дальше?**
Глава 2 — более подробно об инструментах (SSDT, Import/Export Wizard). Если готовы, переходим к ней.

---

✅ **Итог по Главе 1:**  
Отличное введение, которое задаёт фундамент. Ключевое — **архитектура SSIS** (Control Flow vs Data Flow). Если вы это поняли — вы уже на 30% готовы к собеседованию.
