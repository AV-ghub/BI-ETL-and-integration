В реальном мире данные редко живут в одном SQL Server. Эта глава — руководство по работе с «внешним миром»: от простых Excel-файлов до веб-сервисов и Oracle.

---

## **Глава 13: Доступ к гетерогенным данным (Accessing Heterogeneous Data)**

Эта глава — **справочник по подключению SSIS к разнообразным источникам данных**. Авторы разбирают особенности, подводные камни и лучшие практики работы с каждым типом источника. Это must-know для любого ETL-разработчика, работающего в корпоративной среде.

---

### 📌 **Основные категории источников, рассматриваемые в главе:**

1.  **Файловые источники:** Excel, Access, текстовые (плоские) файлы.
2.  **Реляционные СУБД:** Oracle (как пример сторонней СУБД), через ODBC.
3.  **Сетевые/веб-источники:** XML, веб-сервисы.
4.  **Прочие источники через ODBC.**

---

### **1. Excel и Access**

#### **A. 64-битная поддержка (64-Bit Support) — КРИТИЧЕСКИ ВАЖНО**
- **Проблема:** Драйверы для Excel и Access (`Microsoft.ACE.OLEDB.12.0` или `Microsoft.Jet.OLEDB.4.0`) по умолчанию **32-битные**.
- **Симптом:** Пакет, работающий в 64-битном режиме SSIS (по умолчанию в продакшене), **упадет с ошибкой** "The 'Microsoft.ACE.OLEDB.12.0' provider is not registered...".
- **Решение:**
    1.  **Установить 64-битные драйверы** на сервере, где запускается SSIS. Их нужно скачивать отдельно (Microsoft Access Database Engine 2010 Redistributable).
    2.  **Или** запускать пакет в **32-битном режиме**. В SSDT: Project Properties → Debugging → Run64BitRuntime = `False`. В продакшене: в SQL Agent Job Step или `dtexec` использовать ключ `/X86`.

#### **B. Работа с файлами Excel**
- **Источник:** **Excel Source**.
- **Особенности:**
    - Типы данных **определяются по первым 8 строкам** (по умолчанию). Если дальше идут другие типы — ошибка. Можно изменить это в свойствах соединения (`TypeGuessRows`).
    - Имена столбцов берутся из **первой строки** листа. Если первая строка пустая, будут имена типа `F1`, `F2`.
    - Для чтения можно указать лист (`Sheet1$`) или именованный диапазон (`MyNamedRange`).
- **Проблемы:** Нестабильные типы данных, медленная работа с большими файлами.
- **Рекомендация:** Для production-загрузок больших объемов **конвертируйте Excel в CSV** (с помощью скрипта или задачи) и загружайте как Flat File.

#### **C. Работа с базами Access**
- **Источник:** **OLE DB Source** с провайдером `Microsoft.ACE.OLEDB.12.0` (для `.accdb`) или `Microsoft.Jet.OLEDB.4.0` (для `.mdb`).
- **Особенности:** Аналогичны Excel: проблемы с типами данных, драйверами.
- **Ограничение:** Access как источник для больших ETL — плохая идея из-за производительности и надёжности. Рассмотрите миграцию данных в SQL Server.

---

### **2. Импорт из Oracle**
- **Подход:** Использование **OLE DB Source** или **ADO.NET Source**.
- **Предварительные шаги:**
    1.  **Установить Oracle Client** на машине с SSIS. Это может быть полноценный Oracle Client или более легкий **Oracle Data Provider for .NET (ODP.NET)**.
    2.  **Настроить TNSNames** (файл `tnsnames.ora`) для указания алиасов подключения, или использовать прямую строку подключения.
- **Конфигурация в SSIS:** Создать OLE DB Connection Manager, выбрать провайдер `Oracle Provider for OLE DB`. Указать сервер, логин, пароль.
- **Важно:** Разрешения брандмауэра, корректность версий клиента — частые источники проблем.

---

### **3. Использование XML и веб-сервисов**
Авторы связывают эту тему с рассмотренными ранее задачами (Web Service Task, XML Task, XML Source), но дают консолидированный view.

#### **A. Конфигурация задачи веб-сервиса (Web Service Task)**
- **HTTP Connection Manager:** Указывает на WSDL-файл или конечную точку сервиса.
- **Пример из книги:** Получение данных по почтовому индексу из публичного сервиса.
- **Итог:** Web Service Task получает **XML-ответ**, который обычно нужно сохранить в файл или переменную для последующего парсинга.

#### **B. Работа с XML-данными как с источником (XML Source)**
- **Основная проблема:** XML-файл **не содержит информации о типах данных и размерах** (в отличие от реляционной таблицы).
- **Решение:**
    1.  Использовать встроенную в XML **XSD-схему** (Inline Schema).
    2.  **Сгенерировать XSD** на основе примера XML-файла (кнопка "Generate XSD" в редакторе XML Source). **После генерации XSD необходимо проверить и поправить типы данных вручную!**
- **Как использовать:** После получения XML (например, из веб-сервиса) → сохранить в файл → в Data Flow использовать **XML Source**, указав этот файл и XSD-схему → дальше работать с данными как с обычным потоком.

---

### **4. Текстовые (плоские) файлы (Flat Files)**
Подробно рассмотрены в Главе 5, но здесь авторы делают акцент на **особенности гетерогенных сценариев**.

#### **A. Загрузка плоских файлов (Loading Flat Files)**
- **Flat File Source** — основной инструмент.
- **Сложности:** Кодировки (ANSI, UTF-8), разделители, квалификаторы текста, пропуски строк.
- **Лучшая практика:** Всегда тщательно тестируйте Connection Manager на реальном файле, используя предпросмотр.

#### **B. Извлечение данных из плоских файлов (Extracting Data from Flat Files)**
- **Особый случай: нестандартные форматы.** Например, файл, где одна строка содержит заголовок, а следующая — детали.
- **Решение:** Иногда требуется **скрипт (Script Component как источник)** для кастомного парсинга, если стандартный Flat File Source не справляется.

---

### **5. ODBC (Open Database Connectivity)**
- **Назначение:** Универсальный драйвер для доступа к данным, когда нет родного OLE DB или ADO.NET провайдера (старые системы, Unix-СУБД).
- **Источник/Назначение в SSIS:** **ODBC Source / ODBC Destination** (появились в более поздних версиях, в 2012 есть).
- **Настройка:**
    1.  Создать **ODBC Connection Manager** (указать DSN или строку подключения).
    2.  Использовать его в ODBC Source.
- **Недостатки (по сравнению с OLE DB):** Часто **менее производительные**, могут быть проблемы с типами данных и поддержкой возможностей SSIS (например, Fast Load).
- **Рекомендация:** **Используйте OLE DB/ADO.NET, если есть возможность.** ODBC — это крайний вариант.

---

### **6. Другие гетерогенные источники**
Авторы упоминают, что по аналогии можно подключиться к **DB2, Sybase, MySQL, PostgreSQL** и другим системам, для которых существует OLE DB/ODBC/ADO.NET драйвер.
- **Ключевой шаг:** Правильная **установка и настройка клиентского ПО и драйверов** на сервере SSIS.

---

### 🎯 **Что важно запомнить из Главы 13:**

1.  **Главный враг — драйверы.** **64-битные драйверы для Excel/Access должны быть установлены на продакшен-сервере.**
2.  **Excel и Access — не для больших и критичных данных.** Конвертируйте в CSV/SQL.
3.  **Для Oracle и других СУБД** нужен их **клиентский софт**, установленный рядом с SSIS.
4.  **Работа с XML почти всегда требует XSD-схемы.** Генерируйте её и проверяйте.
5.  **ODBC — это "последний шанс".** Предпочитайте OLE DB.
6.  **Тестируйте подключения на реальных данных** на раннем этапе, чтобы избежать сюрпризов при развёртывании.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 13:**

*"К вам ежедневно приходит Excel-файл от бизнес-пользователя, который нужно загрузить в SQL Server. Файл каждый день один и тот же, но иногда пользователь добавляет в него новые столбцы. Как вы построите устойчивый процесс загрузки?"*

**Развёрнутый ответ (с учётом подводных камней):**
*"Это сложный сценарий, потому что структура Excel-файла нестабильна. Моё решение состояло бы из нескольких этапов:*
1.  **Стабилизация источника:** Договориться с бизнес-пользователем о фиксированном формате (шаблон Excel) или о том, что новые столбцы добавляются только в конец.
2.  **Техническая реализация:**
    *   Использую **Excel Source**, но понимаю, что при изменении количества столбцов пакет упадёт.
    *   Чтобы сделать процесс устойчивым, **не буду жёстко задавать список столбцов в Source**. Вместо этого, в свойствах соединения установлю **`OpenRowset` как `'Sheet1$'`** (т.е., "читать всё").
    *   **Но:** Это приведёт к появлению "лишних" столбцов с именами типа `F10`, `F11` в Data Flow.
    *   В Data Flow после источника поставлю **Script Component (как преобразование)**, который будет динамически анализировать метаданные входящих колонок (через `Row.Columns` коллекцию) и формировать выходной набор только с нужными мне колонками, игнорируя новые, пока я не внесу изменения в пакет.
    *   **Альтернатива (проще):** Использовать **более гибкий источник — Flat File**. Попросить пользователя сохранять файл как **CSV (текстовый)**. Структура Flat File Connection Manager более устойчива к добавлению столбцов (можно настроить на чтение, например, первых 15 столбцов, а остальные игнорировать).
3.  **Обработка ошибок:** Настрою вывод ошибок Excel Source, чтобы строки с неконвертируемыми данными (например, текст в числовом столбце) попадали в лог, а не ломали весь процесс.
4.  **Продакшен:** Убежусь, что на сервере установлены **64-битные драйверы ACE.OLEDB**, или настрою запуск пакета в 32-битном режиме через SQL Agent."

---

✅ **Итог по Главе 13:**
Эта глава снимает страх перед **«чужими» системами**. Она даёт чёткий алгоритм: 1) Найти и установить правильный драйвер, 2) Создать Connection Manager, 3) Учесть особенности типа данных, 4) Протестировать. Главный урок: **проблемы с гетерогенными источниками чаще всего лежат не в плоскости SSIS, а в корректности настройки окружения (драйверы, клиенты, права доступа).**
