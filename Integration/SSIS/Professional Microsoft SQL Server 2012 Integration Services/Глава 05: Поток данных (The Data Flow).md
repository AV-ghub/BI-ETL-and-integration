## **Глава 5: Поток данных (The Data Flow)**

Эта глава — это **исчерпывающее руководство по компонентам Data Flow**. Авторы подробно разбирают каждый источник, преобразование и назначение, объясняя их назначение, конфигурацию и внутреннюю работу. Понимание этой главы — это 70% успеха на собеседовании по SSIS.

---

### 📌 **Структура Data Flow (Потока данных):**
1.  **Источники (Sources):** Компоненты, которые читают данные из внешних систем.
2.  **Преобразования (Transformations):** Компоненты, которые модифицируют, очищают, агрегируют и обогащают данные *по мере их движения*.
3.  **Назначения (Destinations):** Компоненты, которые записывают данные в целевые системы.
4.  **Пути (Paths):** Соединяют компоненты, передавая данные и их метаданные (столбцы, типы данных).

**Ключевая концепция:** Data Flow — это **конвейер в памяти (in-memory pipeline)**. Данные не сбрасываются на диск между преобразованиями (за редкими исключениями), что обеспечивает высочайшую производительность.

---

### **1. Просмотр данных (Data Viewers)**
- **Инструмент отладки.** Позволяет "заглянуть" в данные, протекающие по пути между компонентами, в момент выполнения пакета в SSDT.
- Можно просматривать сетку, гистограммы, диаграммы рассеяния.
- **Только для разработки.** Не используется в production.

---

### **2. Источники (Sources)**

Источники подключаются к внешним системам через **Connection Managers**.

| Источник | Назначение | Ключевые моменты |
| :--- | :--- | :--- |
| **OLE DB Source** | Чтение из любых OLE DB источников (SQL Server, Oracle, Access, DB2). | Самый распространённый. Поддерживает таблицу/представление или SQL-запрос. |
| **Excel Source** | Чтение данных из листов или именованных диапазонов Excel. | **Требует наличия драйверов на сервере.** Типы данных определяются по первым нескольким строкам. |
| **Flat File Source** | Чтение из текстовых файлов (CSV, с фиксированной шириной). | Требует тщательной настройки **Flat File Connection Manager** (разделители, типы данных, кодовые страницы). |
| **Raw File Source** | Чтение из специального **родного бинарного формата SSIS**. | **Самый быстрый** источник. Используется как промежуточное хранилище данных между этапами ETL. Не требует парсинга. |
| **XML Source** | Чтение данных из XML-документа. | Требует **XSD-схемы** или использования встроенной схемы (Inline Schema). |
| **ADO.NET Source** | Аналог OLE DB Source для поставщиков ADO.NET (например, .NET-провайдеров для PostgreSQL, MySQL). | Внутренне использует `DataReader`. |
| **CDC Source** (Enterprise) | Чтение изменённых данных из таблиц, включённых в **Change Data Capture (CDC)**. | Для инкрементальной загрузки. Показывает новые, изменённые и удалённые строки. |
| **ODBC Source** | Чтение через ODBC-драйвер. | Используется, когда нет OLE DB/ADO.NET провайдера. |

---

### **3. Назначения (Destinations)**
Назначения записывают данные во внешние системы.

| Назначение | Назначение | Ключевые моменты |
| :--- | :--- | :--- |
| **OLE DB Destination** | Запись в OLE DB источники. | Поддерживает режим **Fast Load** (блочная вставка) для максимальной скорости в SQL Server. |
| **Flat File Destination** | Запись в текстовые файлы. | |
| **Excel Destination** | Запись в Excel. | **Медленное.** Только для небольших объёмов. |
| **Raw File Destination** | Запись в родной бинарный формат SSIS. | **Самый быстрый** способ сохранить промежуточные данные для последующей обработки. |
| **Recordset Destination** | Запись данных в **переменную** типа `Object` (ADO.NET Recordset). | Данные становятся доступны в **Control Flow** (например, для Foreach ADO Enumerator). |
| **SQL Server Destination** | Специальное назначение *только* для SQL Server. | **Устаревшее.** Работает только если SSIS выполняется *на том же сервере*, что и БД. **Используйте OLE DB Destination.** |
| **Dimension / Partition Processing** (Enterprise) | Прямая загрузка и обработка объектов **Analysis Services** (кубы, измерения). | Исключает промежуточные шаги. |
| **Data Mining Training** (Enterprise) | Обучение моделей интеллектуального анализа данных. | |

---

### **4. Преобразования (Transformations) — САМАЯ ВАЖНАЯ ЧАСТЬ**

Авторы делят преобразования на **Common** (часто используемые) и **Other** (специализированные). Ключевое разделение — по **типу работы с памятью**.

#### **A. Синхронные (Synchronous) vs Асинхронные (Asynchronous) преобразования**
| Тип | Принцип работы | Влияние на производительность | Примеры |
| :--- | :--- | :--- | :--- |
| **Синхронные (Non-Blocking)** | Обрабатывают строку "на лету". **Не меняют количество строк.** Входной и выходной буфер — один и тот же. | **Минимальное.** Самые быстрые. | Derived Column, Lookup (в режиме полного кэша), Conditional Split, Data Conversion, Copy Column. |
| **Асинхронные (Blocking)** | Должны **прочитать все строки**, прежде чем выдать первую на выход. **Меняют количество/структуру строк.** Создают новый буфер. | **Высокое.** Могут стать узким местом. Требуют всей доступной памяти. | Aggregate, Sort, Merge (требует сортировки), Merge Join (требует сортировки). |
| **Полу-асинхронные (Semi-Blocking)** | Могут выдавать строки до чтения всех входных данных, но требуют дополнительной памяти и работы. | **Среднее.** | Fuzzy Lookup, Fuzzy Grouping. |

**Для собеседования:** *"Чтобы оптимизировать Data Flow, я минимизирую количество блокирующих (асинхронных) преобразований, таких как Sort и Aggregate. Если сортировка нужна для Merge Join, стараюсь сделать её на уровне источника (ORDER BY в SQL), а не в SSIS."*

#### **B. Основные преобразования (Common Transformations)**

| Преобразование | Назначение | Тип | Важные нюансы |
| :--- | :--- | :--- | :--- |
| **Lookup** | Сопоставление входящих данных со справочной таблицей (поиск значения). | Синхронное (обычно) | **Режимы кэширования:** <br>• **Full Cache** (весь справочник в память) — самый быстрый, если справочник помещается в память. <br>• **Partial Cache** (кэширует только найденные значения) — баланс между памятью и производительностью. <br>• **No Cache** (запрос к БД на каждую строку) — медленно, только для очень больших справочников. |
| **Conditional Split** | Направляет строки по разным выходным путям в зависимости от условия. | Синхронное | Аналог оператора `CASE` или `IF`. |
| **Derived Column** | Добавляет новый вычисляемый столбец или заменяет существующий. | Синхронное | Использует **язык выражений SSIS**. Основа для многих преобразований данных. |
| **Aggregate** | Выполняет группировку и агрегатные функции (SUM, COUNT, AVG, MIN, MAX). | Асинхронное (Blocking) | **Требует всей памяти.** Для больших данных лучше делать агрегацию в источнике (SQL). |
| **Sort** | Сортирует данные. | Асинхронное (Blocking) | **Удаляет дубликаты** (опция). Требует всей памяти. |
| **Union All** | Объединяет несколько потоков данных в один. | Синхронное | Не путать с **Merge** (требует сортировки). Union All просто складывает строки. |
| **Data Conversion** | Изменяет тип данных столбца. | Синхронное | Создаёт *новый* столбец. Может влиять на производительность. |
| **Row Count** | Сохраняет количество прошедших строк в **переменную**. | Синхронное | Переменная становится доступна после завершения работы Data Flow Task в Control Flow. |
| **Slowly Changing Dimension (SCD)** | Автоматизирует загрузку измерений в хранилище данных по типам изменений (Type 1, 2, 3). | Комплексное | Удобный мастер, но часто его заменяют на ручную реализацию для большей гибкости и контроля. |
| **Script Component** (как преобразование) | Позволяет писать произвольную логику обработки строк на C#/VB.NET. | Может быть любым | Мощный инструмент, когда штатных преобразований недостаточно. |

#### **C. Специализированные преобразования (Other Transformations)**

| Преобразование | Назначение (Enterprise Edition) | Тип |
| :--- | :--- | :--- |
| **Fuzzy Lookup** | "Нечёткий" поиск по справочнику (нахождение похожих строк, исправление опечаток). | Полу-асинхронное |
| **Fuzzy Grouping** | Выявление и группировка дубликатов *внутри* набора данных. | Полу-асинхронное |
| **Term Extraction** | Извлечение ключевых слов (существительных, фраз) из текстовых столбцов. | Асинхронное |
| **Term Lookup** | Поиск извлечённых терминов в справочной таблице. | Синхронное |
| **DQS Cleansing** | Очистка данных с помощью **Data Quality Services** и заданных в DQS бизнес-правил. | Зависит от DCS |
| **Pivot / Unpivot** | Преобразование данных из строк в столбцы (Pivot) и обратно (Unpivot). | Асинхронное |
| **Merge / Merge Join** | Объединение **двух отсортированных** потоков. Merge — конкатенация, Merge Join — соединение (INNER, LEFT, FULL). | Асинхронное (требует сортировки) |
| **Multicast** | Создаёт **несколько идентичных копий** потока данных для параллельной обработки. | Синхронное |
| **OLE DB Command** | Выполняет SQL-команду (например, UPDATE, DELETE) **для каждой пришедшей строки**. **ROW-BY-ROW!** | Синхронное (но медленное) |
| **Export / Import Column** | Сохранение данных из столбца (например, изображения) в файл или чтение файла в столбец. | Синхронное |

**Предупреждение про OLE DB Command:** Это преобразование выполняет отдельный SQL-запрос **для каждой строки**. Для обновления/удаления тысяч строк это убийственно медленно. **Лучшая практика:** Направлять такие строки в **staging-таблицу**, а затем выполнять одно массовое обновление с помощью **Execute SQL Task** в Control Flow.

---

### **5. Пример Data Flow из книги**
Авторы показывают построение типичного потока:
1.  **OLE DB Source** читает данные.
2.  **Conditional Split** разделяет поток на "валидные" и "невалидные" данные.
3.  Основной поток проходит через **Derived Column** (добавление вычисляемого поля) и **Lookup** (обогащение справочными данными).
4.  **Aggregate** подсчитывает итоги.
5.  Результаты записываются в **OLE DB Destination**.
6.  Поток с ошибками направляется в **Flat File Destination** для последующего анализа.

---

### 🎯 **Что важно запомнить из Главы 5:**

1.  **Data Flow — это in-memory pipeline.** Производительность заложена в архитектуре.
2.  **Типы преобразований:** Синхронные (быстрые) vs Асинхронные (блокирующие, медленные). **Сортируйте и агрегируйте в БД, если возможно.**
3.  **Lookup — король преобразований.** Понимание разницы между Full/Partial/No Cache — обязательно.
4.  **OLE DB Command — антипаттерн** для массовых операций. Используйте Staging + Execute SQL Task.
5.  **Raw File Source/Destination** — ваш друг для промежуточного сохранения данных на диск без потери производительности.
6.  **Порядок преобразований важен.** Фильтруйте (Conditional Split) и уменьшайте объём данных как можно раньше.
7.  **Многие мощные преобразования (Fuzzy, DQS, Term)** требуют **Enterprise Edition**.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 5:**

*"У вас есть поток в 10 млн строк. Для каждой строки нужно найти соответствие в справочной таблице на 1 млн записей. Как вы настроите Lookup Transformation?"*

**Развёрнутый ответ:**
1.  **Проанализирую данные.** Если справочная таблица помещается в память (1 млн строк * средний размер строки), выберу **Full Cache**. Это даст максимальную скорость.
2.  Если справочник слишком велик, но большинство входящих строк находят соответствие, выберу **Partial Cache**. SSIS будет кэшировать только найденные значения, экономя память.
3.  **No Cache** выберу только в крайнем случае, если память ограничена, а процент попаданий очень низкий. Это будет медленно.
4.  **Дополнительные оптимизации:**
    *   В источнике Lookup оставлю только нужные для сопоставления столбцы (ключи) и возвращаемые значения, чтобы уменьшить размер кэша.
    *   Если возможно, **предварительно отфильтрую** основной поток, чтобы уменьшить количество строк для поиска.
    *   Рассмотрю вариант с **Cache Connection Manager + Cache Transform**, если один и тот же справочник используется в нескольких потоках или пакетах.

---

✅ **Итог по Главе 5:**
Это **библия Data Flow**. Здесь сосредоточена вся мощь SSIS как ETL-инструмента. Главный вывод: SSIS предоставляет невероятно богатый набор оптими
