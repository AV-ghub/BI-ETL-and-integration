**Глава 20: External Management and WMI Task Implementation** посвящена двум ключевым темам: **программному управлению SSIS из .NET-кода** и **использованию мощных WMI-задач для мониторинга системы**.

---

## **Глава 20: Внешнее управление и реализация задач WMI**

Эта глава — для администраторов, DevOps-инженеров и разработчиков, которые хотят **встраивать SSIS в более крупные автоматизированные процессы** или создавать инструменты для управления ETL-инфраструктурой.

---

### 📌 **Структура главы:**

1.  **Внешнее управление SSIS с помощью управляемого кода (External Management of SSIS with Managed Code).**
2.  **Задачи Windows Management Instrumentation (WMI Tasks).**

---

### **1. Внешнее управление SSIS с помощью управляемого кода**

Авторы показывают, как использовать **.NET API (объектную модель) SSIS** для выполнения операций, которые обычно делаются через SSMS или `dtexec`: развёртывание проектов, выполнение пакетов, настройка параметров, мониторинг.

#### **A. Объектная модель управляемого кода (The Managed Object Model Code Library)**
Для работы с SSIS из .NET используются в основном **две ключевые сборки** (пространства имён `Microsoft.SqlServer.*`):
1.  **`Microsoft.SqlServer.Management.IntegrationServices`:** Для работы с **каталогом SSISDB (Project Deployment Model)**. Управление проектами, папками, окружениями, выполнениями.
2.  **`Microsoft.SqlServer.Dts.Runtime`:** Для работы с **пакетами напрямую (Package Deployment Model)**, их загрузки, выполнения и управления параметрами/конфигурациями. Также содержит классы для работы со **старой службой SSIS**.

#### **B. Управление каталогом (Catalog Management) — SSISDB**
- **Ключевой объект: `IntegrationServices`** (из пространства имён `Microsoft.SqlServer.Management.IntegrationServices`).
- **Подключение:** Создаётся на основе соединения с SQL Server, где размещён каталог SSISDB.
    ```csharp
    using Microsoft.SqlServer.Management.IntegrationServices;
    // Создаём соединение с SQL Server
    SqlConnection sqlConn = new SqlConnection("Data Source=.;Initial Catalog=master;Integrated Security=SSPI;");
    // Создаём объект для работы с SSIS
    IntegrationServices iss = new IntegrationServices(sqlConn);
    ```
- **Иерархия объектов каталога:**
    - **`IntegrationServices`** → **`Catalog`** (SSISDB) → **`Folder`** → **`Project`** → **`Package`**.
    - Также есть **`Environment`** (окружения) и **`EnvironmentReference`** (ссылки проектов на окружения).

#### **C. Управление папками (Folder Management)**
- Создание, перечисление, удаление папок внутри каталога SSISDB.
    ```csharp
    Catalog catalog = iss.Catalogs["SSISDB"];
    // Создать папку
    catalog.CreateFolder("MyNewFolder", "Описание папки");
    // Получить список папок
    foreach (CatalogFolder folder in catalog.Folders)
    {
        Console.WriteLine(folder.Name);
    }
    ```

#### **D. Управление окружениями (Environments)**
- **Окружение (Environment)** — это контейнер для **переменных**, которые можно использовать для задания значений параметрам проектов/пакетов (например, `ConnectionString` для Dev, Test, Prod).
- **Создание и настройка окружения:**
    ```csharp
    CatalogFolder folder = catalog.Folders["MyNewFolder"];
    // Создать окружение
    EnvironmentInfo env = folder.CreateEnvironment("Production", "Продакшен-окружение");
    // Добавить переменную в окружение
    env.Variables.Add("ServerName", TypeCode.String, "PROD-SQL-01", false, "Имя сервера БД");
    env.Alter(); // Сохранить изменения
    ```

#### **E. Развёртывание проектов (SSIS Deployment Projects)**
- Загрузить файл проекта (`.ispac`) и развернуть его в каталог.
    ```csharp
    byte[] projectBytes = File.ReadAllBytes(@"C:\MyProject\bin\Development\MyETLProject.ispac");
    folder.DeployProject("MyETLProject", projectBytes);
    ```

#### **F. Управление параметрами (Parameter Objects)**
- Получить доступ к параметрам развёрнутого проекта или пакета, прочитать или установить их значения.
    ```csharp
    ProjectInfo project = folder.Projects["MyETLProject"];
    // Получить параметр проекта
    PackageInfo package = project.Packages["Package.dtsx"];
    // Получить параметр пакета
    ```

#### **G. Выполнение пакетов, развёрнутых в каталог SSIS (Executing SSIS Packages Deployed to the SSIS Catalog)**
- **Ключевой объект: `PackageInfo`.** У него есть метод `Execute`.
- Можно установить **значения параметров** и **ссылку на окружение** для выполнения.
    ```csharp
    // Создать объект для настройки выполнения
    Collection<PackageInfo.ExecutionValueParameterSet> executionParams = new Collection<PackageInfo.ExecutionValueParameterSet>();
    // Установить значение параметру (например, переопределить значение из окружения)
    executionParams.Add(new PackageInfo.ExecutionValueParameterSet { ObjectType = 30, ParameterName = "MyParameter", ParameterValue = "MyValue" });
    
    // Выполнить пакет (синхронно)
    long executionId = package.Execute(false, null, executionParams);
    // executionId можно использовать для мониторинга выполнения
    ```

#### **H. Мониторинг выполнения (Package Monitoring)**
- Через каталог SSISDB можно получить детальную информацию о выполнении: статус, сообщения, предупреждения, ошибки.
    ```csharp
    // Получить объект выполнения по ID
    OperationInfo operation = catalog.Operations[executionId];
    Console.WriteLine($"Status: {operation.Status}");
    // Получить сообщения выполнения
    foreach (OperationMessage message in operation.Messages)
    {
        Console.WriteLine($"{message.MessageType}: {message.Message}");
    }
    ```

#### **I. Пример: Приложение для управления пакетами (A Package Management Example)**
Авторы приводят пример консольного приложения, которое:
1.  Подключается к каталогу SSISDB.
2.  Перечисляет все проекты и пакеты.
3.  Предоставляет меню для выполнения выбранного пакета с возможностью указать значения параметров.
4.  Отслеживает ход выполнения и выводит результаты.

**Для собеседования:** *"Для автоматизации развёртывания и тестирования мы используем .NET-скрипты на базе `Microsoft.SqlServer.Management.IntegrationServices`. Наш CI/CD-пайплайн в Azure DevOps после сборки проекта SSIS (.ispac) разворачивает его в тестовый каталог SSISDB, устанавливает параметры через API, запускает пакеты и анализирует логи выполнения через `catalog.operation_messages`, чтобы автоматически определять успешность теста."*

---

### **2. Задачи Windows Management Instrumentation (WMI Tasks)**

WMI — это **инфраструктура управления Windows**, которая предоставляет единый интерфейс для запроса и получения уведомлений о практически любом аспекте системы: оборудование, установленное ПО, процессы, события.

В SSIS есть **две задачи для работы с WMI**:
1.  **WMI Data Reader Task** — для **запроса** информации (получить список файлов, процессов).
2.  **WMI Event Watcher Task** — для **ожидания и реакции** на события (появление файла, завершение процесса).

#### **A. Задача "Чтение данных WMI" (WMI Data Reader Task Explained)**
- **Назначение:** Выполнить **WQL-запрос** (WMI Query Language, похож на SQL) к WMI и сохранить результат.
- **Примеры WQL:**
    - Получить список запущенных процессов: `SELECT * FROM Win32_Process`
    - Получить список файлов в папке: `SELECT * FROM CIM_DataFile WHERE Drive='C:' AND Path='\\temp\\'`
    - Получить информацию о дисках: `SELECT * FROM Win32_LogicalDisk`
- **Куда сохраняется результат:** В **переменную** типа `Object` (набор строк) или в файл (XML, текстовый).
- **Использование результата:** Если результат сохранён в переменную, его можно прочитать в **Foreach Loop Container** (с перечислителем `Foreach ADO Enumerator`) или в **Script Task**.

#### **B. Пример: Задача чтения данных WMI (WMI Data Reader Example)**
**Сценарий:** Получить список всех `.txt` файлов в указанной папке для последующей обработки.
1.  Настроить WMI Data Reader Task.
2.  **WqlQuerySource:** `SELECT * FROM CIM_DataFile WHERE Drive='C:' AND Path='\\data\\incoming\\' AND Extension='txt'`
3.  **OutputType:** `DataTable` (сохранить в переменную).
4.  **OverwriteDestination:** `True`.
5.  Назначить переменную (например, `User::FileList`).
6.  После этой задачи поставить **Foreach Loop Container** (с перечислителем `Foreach ADO Enumerator`), который будет итерироваться по переменной `User::FileList` и для каждого файла выполнять Data Flow Task для его загрузки.

#### **C. Задача "Наблюдатель за событиями WMI" (WMI Event Watcher Task)**
- **Назначение:** **Ожидать наступления определённого события WMI**, после чего продолжить выполнение пакета.
- **Принцип работы:** Пакет **приостанавливает выполнение** на этой задаче и "засыпает", пока не произойдёт указанное событие. Затем задача завершается успешно, и поток идёт дальше.
- **Примеры событий:**
    - Создание файла: `SELECT * FROM __InstanceCreationEvent WITHIN 10 WHERE TargetInstance ISA 'CIM_DataFile' AND TargetInstance.Drive='C:' AND TargetInstance.Path='\\temp\\'`
    - Завершение процесса: `SELECT * FROM Win32_ProcessStopTrace`
- **Важные настройки:**
    - **Timeout:** Через какое время ожидания завершить задачу с ошибкой, если событие не произошло.
    - **ActionAtTimeout, AfterEvent, AtTimeout:** Что делать при наступлении события или таймауте (завершить задачу успешно/неуспешно).

#### **D. Пример: Задача наблюдателя за событиями WMI (WMI Event Watcher Task Example)**
**Сценарий:** Ожидать появления файла `trigger.txt` в папке `C:\incoming`, после чего начать основную обработку данных.
1.  Настроить WMI Event Watcher Task.
2.  **WqlQuerySource:** `SELECT * FROM __InstanceCreationEvent WITHIN 10 WHERE TargetInstance ISA 'CIM_DataFile' AND TargetInstance.Drive='C:' AND TargetInstance.Path='\\incoming\\' AND TargetInstance.FileName='trigger' AND TargetInstance.Extension='txt'`
3.  **Timeout:** 3600 (секунд, 1 час). Если файл не появится за час, задача завершится с ошибкой (`ActionAtTimeout = FailTask`).
4.  После успешного завершения задачи (файл появился) выполняется основная ETL-логика.

---

### 🎯 **Что важно запомнить из Главы 20:**

1.  **SSIS можно полностью управлять из .NET-кода.** Это открывает возможности для создания **собственных инструментов администрирования, CI/CD-пайплайнов и панелей мониторинга**.
2.  **Для каталога SSISDB используйте `Microsoft.SqlServer.Management.IntegrationServices`.** Это основной API для Project Deployment Model.
3.  **WMI-задачи — мощный мост между SSIS и операционной системой.** 
    - **WMI Data Reader** — для **активного опроса** системы (что сейчас запущено? какие файлы есть?).
    - **WMI Event Watcher** — для **реакции на события** (когда файл появится? когда служба упала?). Позволяет создавать пакеты, которые запускаются **по событию**, а не по расписанию.
4.  **Результат WMI Data Reader можно передать в цикл Foreach** для построчной обработки (очень мощный паттерн!).
5.  **WQL нужно знать.** Это ключ к эффективному использованию WMI-задач.

---

### ❓ **Пример вопроса на собеседовании из темы Главы 20:**

*"Как бы вы автоматизировали процесс развёртывания новой версии ETL-проекта из пайплайна CI/CD (например, из Azure DevOps) на тестовый сервер SSIS и его последующий запуск с тестовыми параметрами?"*

**Развёрнутый ответ (использующий API управления):**
*"Мы бы использовали PowerShell-скрипт или .NET Core-приложение в рамках этапа Release нашего пайплайна. Скрипт выполнял бы следующие шапы через API `Microsoft.SqlServer.Management.IntegrationServices`:*
1.  **Подключение:** Установить соединение с SQL Server, где размещён тестовый каталог SSISDB.
2.  **Развёртывание:** Загрузить артефакт сборки — файл `.ispac` — и развернуть его в нужную папку каталога SSISDB методом `folder.DeployProject()`. При необходимости сначала удалить старую версию.
3.  **Настройка окружения:** Убедиться, что в папке существует тестовое окружение (`Environment`) с переменными (строка подключения к тестовой БД, пути к файлам). При необходимости создать или обновить его.
4.  **Настройка ссылки:** Убедиться, что развёрнутый проект ссылается на это тестовое окружение.
5.  **Выполнение:** Запустить ключевые пакеты проекта методом `package.Execute()`, явно передав при необходимости какие-то специфичные тестовые значения параметров через `ExecutionValueParameterSet`.
6.  **Мониторинг и валдация:** В цикле опрашивать статус выполнения через `catalog.Operations[executionId]`. Дождаться успешного завершения. Проанализировать сообщения выполнения (`operation.Messages`) на наличие предупреждений или ошибок. В случае неудачи — завершить пайплайн с ошибкой.
*Этот подход обеспечивает полностью автоматизированное, контролируемое и повторяемое развёртывание и тестирование ETL-логики."*

---

✅ **Итог по Главе 20:**
Эта глава показывает SSIS как **открытую, интегрируемую платформу**, а не как изолированный инструмент. Умение управлять SSIS программно и реагировать на события системы через WMI превращает набор отдельных пакетов в **управляемую, событийно-ориентированную ETL-инфраструктуру предприятия**, которую можно встроить в современные DevOps-процессы.
