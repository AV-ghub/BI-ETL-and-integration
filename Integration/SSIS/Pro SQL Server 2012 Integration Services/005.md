## 📘 **Шпаргалка по SSIS: Переменные, Параметры, Выражения (Глава 9)**

### 🔹 **Переменные (Variables)**
- **Назначение:** временное хранение значений в памяти для использования в пакете.
- **Типы переменных:**
  - **Системные (System):** только для чтения, автоматически создаются SSIS (например, `System::StartTime`, `System::PackageName`).
  - **Пользовательские (User-defined):** создаются разработчиком, могут быть скалярными или табличными.
- **Области видимости:** уровень пакета, контейнера, задачи.
- **Чувствительность к регистру:** да, имена переменных чувствительны к регистру.

### 🔹 **Параметры (Parameters)**
- **Назначение:** замена конфигурационных файлов в SSIS 2012+, передача значений извне пакета.
- **Уровни параметров:**
  - **Пакетные (Package-level):** передаются между родительскими и дочерними пакетами.
  - **Проектные (Project-level):** общие для всех пакетов проекта.
- **Свойства параметров:**
  - `Name`, `Data Type`, `Value` (значение по умолчанию),
  - `Sensitive` (шифрование, например, для паролей),
  - `Required` (обязательный для выполнения).
- **Привязка параметров:** можно привязать к свойствам задач, контейнеров, менеджеров соединений (через свойства `ConnectionString`).

### 🔹 **Выражения (Expressions)**
- **Назначение:** динамическое вычисление значений на основе переменных, параметров, функций и операторов.
- **Используются для:**
  - Присвоения значений переменным.
  - Управления свойствами задач (например, `DelayValidation`).
  - Условной логики (аналог `IF…THEN…ELSE` через `? :`).
- **Expression Builder:** инструмент для построения и проверки выражений.
- **Пример выражения:**
  ```
  GETDATE() > DATEADD("Second", 2, @[System::StartTime]) ? TRUE : FALSE
  ```
  → Проверяет, прошло ли 2 секунды с начала выполнения пакета.

### 🔹 **Ключевые отличия:**
| | **Переменные (Variables)** | **Параметры (Parameters)** |
|----------------|----------------------------|----------------------------|
| **Изменяемость** | Да (кроме системных) | Только при выполнении/развёртывании |
| **Область видимости** | Пакет/контейнер/задача | Пакет или проект |
| **Конфигурация** | Внутри пакета | Внешняя настройка (деплоймент) |
| **Чувствительные данные** | Можно пометить как `Sensitive` | Есть свойство `Sensitive` |

### 🔹 **Аудиторские преобразования (Auditing)**
- **Row Count:** подсчёт строк в потоке данных, результат сохраняется в переменную.
  - ⚠️ **Важно:** переменная обновляется только **после завершения потока данных**.
- **Audit:** добавление системных переменных (например, время выполнения, имя пакета) как новых столбцов в поток.
  - Альтернатива: можно использовать `Derived Column`.

### 🔹 **Business Intelligence Transformations**
- Преобразования для **очистки данных** и **валидации**.
- **Slowly Changing Dimension (SCD):** для работы с изменяющимися измерениями в хранилищах данных (тема отдельной главы).

---

### 🧠 **Что важно запомнить для собеседования:**
1. **Параметры пришли на смену конфигурациям** в SSIS 2012.
2. **Системные переменные только для чтения**, пользовательские — гибкие.
3. **Выражения** — мощный инструмент для динамического управления пакетом, но их стоит делать простыми и читаемыми.
4. **Row Count** обновляет переменную **после** завершения потока данных.
5. **Audit** — удобный способ добавить метаданные выполнения в поток.

---
