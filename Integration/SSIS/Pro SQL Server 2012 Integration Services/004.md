## 📘 **Рекап: Главы 7 и 8 – Источники, назначения и преобразования данных**

### 📌 **Часть 1: Источники и назначения баз данных (Database Sources and Destinations)**

#### 1. **OLE DB Source & Destination**
- **Источник:** Подключение к SQL Server и другим БД через OLE DB.
- **Режимы доступа:**
  - `Table or View` – выбирает всю таблицу/представление (как `SELECT *`).
  - `SQL Command` – позволяет писать свой SQL (лучшая практика для производительности и выбора только нужных столбцов).
  - `SQL Command from Variable` – SQL-запрос хранится в переменной.
  - `Table Name or View Name Variable` – имя таблицы в переменной.
- **Параметризованные запросы:** Используется `?` как плейсхолдер. Параметры передаются по порядку (`Parameter0`, `Parameter1`…).

#### 2. **OLE DB Destination**
- **Режимы доступа:**
  - `Table or View – Fast Load` – использует **Bulk Insert** через `IRowsetFastLoad` (высокая производительность, пакетная вставка).
  - `Table or View` – по одной строке (`INSERT` на строку, медленно).
  - Аналогичные варианты с переменными.
- **Важные настройки Fast Load:**
  - `Keep Identity` – вставка в IDENTITY-столбец.
  - `Keep Nulls` – вставка NULL, даже если есть DEFAULT.
  - `Table Lock` – блокировка таблицы на время загрузки.
  - `Rows per Batch` / `Max Insert Commit Size` – размер пакета (рекомендуется 50 000–100 000 строк).

#### 3. **ADO.NET Source & Destination**
- Управляемый .NET-провайдер (`SqlClient`).
- **Проще в использовании из .NET-кода** (скрипты), но обычно **медленнее OLE DB**.
- **Не поддерживает параметризованные запросы** в источнике (но можно задать выражение для `SqlCommand`).
- В назначении есть опция `Use Bulk Insert When Possible`.

#### 4. **SQL Server Destination**
- Только для **локального SQL Server** (использует общую память).
- Высокоскоростная **Bulk Insert** прямо из потока данных.
- Нельзя использовать для удалённых серверов.

#### 5. **SQL Server Compact Destination**
- Для загрузки в **SQL Server Compact Edition**.
- **Только 32-битный драйвер**.

---

### 📌 **Часть 2: Файловые источники и назначения (Files)**

#### 1. **Flat File Source/Destination**
- Работа с текстовыми файлами: с разделителями, фиксированной шириной, «рваный правый край».
- **Flat File Connection Manager** определяет формат, кодировку, разделители.
- **Advanced Editor** позволяет тонко настраивать маппинг и метаданные столбцов.

#### 2. **Excel Source/Destination**
- Источник данных из Excel-файлов через **Jet/ACE provider**.
- **Только 32-битный драйвер** → нужно запускать пакет в **32-битном режиме** (`Run64BitRuntime = False`).
- Автоматическое определение типов данных (часто ошибается → может потребоваться `Data Conversion` или `Derived Column`).
- **Режимы доступа:** `Table/View` (лист или именованный диапазон) или `SQL Command` (ограниченный синтаксис).

#### 3. **Raw File Source/Destination**
- **Бинарный формат SSIS** для промежуточного хранения данных.
- **Высокая производительность** (нет конвертаций, данные хранятся в native-формате).
- Используется для передачи данных между пакетами или этапами ETL, особенно при смешивании 32/64-битных драйверов.

#### 4. **XML Source**
- Чтение XML-файлов с использованием **XSD  схемы**.
- Подходит для простых XML или извлечения определённых данных из сложных XML.

---

### 📌 **Часть 3: Преобразования данных (Data Flow Transformations)**

#### 1. **Типы преобразований по способу обработки:**
- **Синхронные (Synchronous):** Обрабатывают строки по одной, используют общий буфер. Быстрые, неблокирующие. Пример: `Derived Column`, `Data Conversion`.
- **Асинхронные (Asynchronous):** Требуют отдельного выходного буфера, могут менять количество строк. Пример: `Sort`, `Aggregate`.
- **Блокирующие (Blocking):**
  - **Полностью блокирующие** – обрабатывают весь набор данных перед выводом (`Sort`, `Aggregate`).
  - **Частично блокирующие** – буферизуют часть данных (`Merge Join`).
  - **Неблокирующие** – сразу передают строки дальше (`Derived Column`).

#### 2. **Преобразования строк (Row Transformations):**
- **Data Conversion** – изменение типа данных столбца.
- **Character Map** – операции со строками (верхний/нижний регистр, замена символов и т.д.).
- **Copy Column** – копирование столбцов.
- **Derived Column** – **одно из самых мощных преобразований**:
  - Вычисление новых столбцов с помощью **SSIS Expression Language**.
  - Поддерживает математические, строковые функции, условия (`?:`), приведение типов.
- **Import/Export Column** – импорт/экспорт больших объектов (LOB) из/в файлы.
- **OLE DB Command** – выполнение SQL-запроса (например, `UPDATE`) для **каждой строки**. **Медленно для больших объёмов** (RBAR – Row-by-Agonizing-Row).
- **Script Component** – кастомная логика на C#/VB. Может быть источником, преобразованием или назначением.

#### 3. **Преобразования наборов строк (Rowset Transformations):**
- **Aggregate** – агрегация и группировка (SUM, COUNT, MIN, MAX, GROUP BY).
- **Sort** – сортировка данных. **Полностью блокирующее**. Важно свойство `IsSorted` и `SortKeyPosition`.
- **Pivot / Unpivot** – сводные таблицы: поворот строк в столбцы и наоборот.
- **Percentage/Row Sampling** – выборка случайных строк (по проценту или количеству).

#### 4. **Разделение и объединение (Splits and Joins):**
- **Conditional Split** – направление строк по условиям (аналог `IF...ELSE`).
- **Multicast** – дублирование потока данных для нескольких выходов.
- **Union All** – объединение нескольких потоков (аналог `UNION ALL` в SQL).
- **Merge** – объединение **отсортированных** потоков с сохранением порядка.
- **Merge Join** – соединение двух отсортированных потоков (`INNER`, `LEFT`, `FULL OUTER JOIN`).
- **Lookup** – **очень важное преобразование**:
  - Поиск данных в справочной таблице.
  - **Режимы кэширования:**
    - **Full Cache** – вся справочная таблица загружается в память (рекомендуется для небольших таблиц).
    - **Partial Cache** – загружаются только нужные строки по мере необходимости.
    - **No Cache** – запрос к БД для каждой строки (медленно).
  - Обработка строк без совпадений: ошибка, игнорировать, перенаправить.
- **Cache Transform** – подготовка данных для `Lookup` из любых источников (не только OLE DB). Данные кэшируются в памяти или файле.

---

### 🧠 **Что важно запомнить для собеседования:**
- **OLE DB Fast Load** – лучший способ для вставки больших объёмов в SQL Server.
- **Derived Column** мощнее `Data Conversion` и `Character Map`.
- **Lookup** – режимы кэширования и обработка отсутствующих совпадений.
- **Sort**, **Aggregate**, **Merge Join** требуют отсортированных данных (`IsSorted = True`).
- **OLE DB Command** – осторожно, RBAR-подход, может быть медленным.
- **Script Component** – для кастомной логики, поддерживает синхронный и асинхронный режим.
- **Raw Files** – эффективный формат для промежуточного хранения.
- **Excel** – только 32-битный драйвер, проблемы с типами данных.

---
