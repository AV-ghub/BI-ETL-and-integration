## 🔍 **Рекап содержания документа: Auditing и Variables, Parameters, Expressions**

### 📌 **Часть 1: Аудит и преобразования аудита**
SSIS включает несколько преобразований для **аудита** — сбора метаданных о пакете или потоке данных.

#### 1. **Row Count (Подсчет строк)**
- Подсчитывает строки, проходящие через него.
- Результат записывается в **переменную типа integer**.
- **Важно:** переменная обновляется только после завершения потока данных.

#### 2. **Audit (Аудит)**
- Позволяет добавить в поток данных **системные переменные** как новые столбцы (например: имя пакета, время выполнения, ID пользователя).
- Можно переименовывать выходные столбцы.
- Альтернатива: можно использовать **Derived Column** для тех же целей.

---

### 📌 **Часть 2: Business Intelligence Transformations**
- Это преобразования для **очистки и валидации данных**.
- **Slowly Changing Dimension** — для работы с измерениями в хранилищах данных.
- Другие преобразования рассматриваются в главе 12.

---

### 📌 **Часть 3: Variables, Parameters, and Expressions (Глава 9)**

#### 1. **Переменные (Variables)**
- **Системные переменные** — предопределены SSIS, только для чтения (например: `System::StartTime`).
- **Пользовательские переменные** — создаются разработчиком, могут быть скалярными или табличными.
- Используются для:
  - Параметризации SQL-запросов
  - Управления контейнерами циклов
  - Хранения временных значений

#### 2. **Параметры (Parameters)**
- Заменяют конфигурационные файлы в новых версиях SSIS.
- **Пакетные параметры** — передача значений между родительскими и дочерними пакетами.
- **Проектные параметры** — доступны всем пакетам проекта.
- Могут быть **чувствительными** (шифруются) и **обязательными**.

#### 3. **Выражения (Expressions)**
- Используются для:
  - Присвоения значений переменным
  - Возврата результатов вычислений
  - Условных операций
- **Expression Builder** — инструмент для построения и проверки выражений.
- Пример: проверка, прошло ли 2 секунды с начала выполнения пакета.

---

### 🧠 **Что важно запомнить для собеседования:**
- **Row Count** обновляет переменную только после завершения потока данных.
- **Audit** — удобный способ добавить метаданные в поток.
- **Системные переменные** — только для чтения, **пользовательские** — гибкие.
- **Параметры** пришли на смену конфигурациям, поддерживают проект и пакет.
- **Выражения** — мощный инструмент для динамического управления пакетом.

---

Если хочешь, можем дальше разобрать примеры использования или типичные вопросы на собеседовании по этим темам.
