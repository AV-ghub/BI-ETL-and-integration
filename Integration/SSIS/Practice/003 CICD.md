# **Как перевыгрузить (обновить) пакет в SSIS Catalog?**

### **Способ 1: Простой Redeploy (Переразвертывание) через SSMS**
(Подходит, когда вы хотите **полностью заменить** текущую версию проекта)

1.  В SSMS: Правой кнопкой по **существующему проекту** в SSISDB → **Deploy Project...**
2.  В мастере на первом шаге выберите **"Browse and select project from file system"** и укажите ваш новый `.ispac` файл (уже собранный с изменениями).
3.  Нажмите "Next" до конца. Мастер предупредит, что проект уже существует и будет перезаписан.

**Что происходит:** Старая версия проекта **не удаляется физически**, она сохраняется в истории версий (подробнее ниже). Новая версия становится **актуальной (current)**.

---

### **Способ 2: Обновление через Integration Services Deployment Wizard**
(Тот же мастер, но запущенный отдельно — `ISDeploymentWizard.exe`)

Полезно для автоматизации через скрипты.

---

### **Способ 3: Развертывание через PowerShell (Для автоматизации)**
```powershell
# Загрузка сборки
Add-Type -AssemblyName "Microsoft.SqlServer.Management.IntegrationServices"

# Подключение к серверу
$sqlConnectionString = "Data Source=YourServer;Initial Catalog=master;Integrated Security=SSPI;"
$sqlConnection = New-Object System.Data.SqlClient.SqlConnection($sqlConnectionString)

$ssisServer = New-Object Microsoft.SqlServer.Management.IntegrationServices.IntegrationServices($sqlConnection)
$catalog = $ssisServer.Catalogs["SSISDB"]
$folder = $catalog.Folders["YourFolder"]
$project = $folder.Projects["YourProject"]

# Развертывание новой версии
$project.Deploy([System.IO.File]::ReadAllBytes("C:\Path\To\YourProject.ispac"))
```

---

### **Способ 4: Развертывание через T-SQL (Самый гибкий для сложных сценариев)**
```sql
USE [SSISDB]
GO

-- Создаем бинарное представление файла .ispac
DECLARE @ProjectBinary AS VARBINARY(MAX)
SET @ProjectBinary = (SELECT * FROM OPENROWSET(BULK 'C:\YourProject.ispac', SINGLE_BLOB) AS BinaryData)

-- Вызываем хранимую процедуру развертывания
EXEC [catalog].[deploy_project] 
    @folder_name = N'YourFolder',
    @project_name = N'YourProject',
    @Project_Stream = @ProjectBinary
```

---

## **🔴 ПОДВОДНЫЕ КАМНИ (ОЧЕНЬ ВАЖНО!)**

### **1. Перезапись конфигураций (Environments, Parameters)**
**Самая большая и опасная ловушка!**

*   **Проблема:** При переразвертывании проекта **ВСЕ ваши ручные настройки в окне Configure (маппинг параметров на Environment Variables, измененные значения параметров) могут быть СБРОШЕНЫ на значения по умолчанию из нового `.ispac`**.
*   **Почему:** Новый `.ispac` содержит "чистые" параметры с дефолтными значениями, как при разработке. При деплое он перезаписывает метаданные.
*   **Решение (КРИТИЧЕСКИ ВАЖНО):**
    1.  **Перед деплоем:** В SSMS откройте текущий проект → **Configure** → вкладка **Parameters**. Нажмите кнопку **"Script"** (вверху окна). Сохраните T-SQL скрипт.
    2.  **После деплоя:** Выполните сохраненный скрипт. Он восстановит все маппинги и кастомные значения.
    3.  **Лучшая практика:** Храните эти скрипты конфигурации в системе контроля версий (Git) вместе с кодом проекта.

### **2. Потеря истории выполнений**
*   **Что не теряется:** Все **предыдущие запуски** (executions) остаются в каталоге и привязаны к старым версиям проекта.
*   **Что может смутить:** В отчетах "All Executions" вы будете видеть запуски и от старой, и от новой версии. Это нормально.

### **3. Работающие выполнения (Running Executions)**
*   **Проблема:** Если вы развертываете проект, пока **старая версия пакета выполняется**, это может привести к непредсказуемым результатам.
*   **Решение:** Перед деплоем проверьте, не выполняется ли сейчас пакет:
    ```sql
    USE [SSISDB]
    SELECT * FROM [catalog].[executions] 
    WHERE status = 2 -- 2 = running
      AND project_name = 'YourProject'
    ```
    Лучше дождаться завершения или остановить выполнение.

### **4. Зависимости от других проектов/пакетов**
*   Если ваш пакет вызывается из других пакетов через **Execute Package Task**, после деплоя ссылки сохранятся (т.к. имя проекта и пакета не меняется).
*   Но если вы **изменили интерфейс** (удалили параметры, которые используются при вызове), вызов сломается.

### **5. Изменение структуры параметров**
*   **Если вы удалили параметр**, который был привязан к Environment Variable, привязка **исчезнет** (и это правильно).
*   **Если вы добавили новый обязательный параметр** без значения по умолчанию, пакет **не запустится**, пока вы не настроите для него значение или привязку в Configure. Проверьте это сразу после деплоя.

### **6. Версионирование (Versioning)**
SSIS Catalog автоматически ведет **историю версий проекта**.
```sql
-- Посмотреть все версии проекта
USE [SSISDB]
SELECT * FROM [catalog].[projects] p
JOIN [catalog].[object_versions] ov ON p.project_id = ov.object_id
WHERE p.name = 'YourProject'
ORDER BY ov.created_time DESC
```
**Важно:** Можно откатиться на предыдущую версию через `[catalog].[restore_project]`, но это **редкая и рискованная операция**.

---

## **Безопасный процесс обновления (Checklist)**

1.  **Собрать новый `.ispac`** в SSDT (Build).
2.  **Экспортировать текущие конфигурации** из Production (кнопка "Script" в Configure).
3.  **Проверить, нет ли запущенных выполнений** пакета.
4.  **Выполнить деплой** новым `.ispac`.
5.  **Применить сохраненный скрипт конфигураций**.
6.  **Верифицировать**, что маппинги восстановились (открыть Configure и проверить).
7.  **Запустить тестовое выполнение** пакета с тестовым Environment (если есть) или с основным, но на ограниченном наборе данных.
8.  **Убедиться**, что все зависимости работают.

---

## **Что происходит с SQL Agent Jobs?**

**Хорошая новость:** Задания SQL Agent, которые запускают ваш пакет, **продолжат работать без изменений**!

*   Job хранит ссылку на **путь к пакету в каталоге** (например, `\SSISDB\Folder\Project\Package.dtsx`).
*   При деплое путь не меняется.
*   Job будет автоматически запускать **последнюю (current) версию** проекта.

**Но проверьте:** Если в Job'е были явно указаны значения параметров (в свойствах шага), они могут быть сброшены. После деплоя загляните в настройки шага Job'а.

---

## **Практическая рекомендация**

**Для критичных production-проектов создайте PowerShell-скрипт, который:**
1.  Останавливает связанные Jobs
2.  Проверяет отсутствие running executions
3.  Делает бэкап конфигураций
4.  Выполняет деплой
5.  Восстанавливает конфигурации
6.  Запускает тестовое выполнение
7.  Возобновляет Jobs

Это избавит вас от многих ночных кошмаров.

# Что произойдет, если вы измените тип данных параметра с string на int в проекте, а в Environment Variable останется строковое значение?
---

### **Что происходит при несовпадении типов данных?**

1.  **В момент запуска пакета**, движок SSIS пытается взять значение из переменной окружения (Environment Variable) и передать его в параметр пакета.
2.  **Движок выполняет неявное преобразование типа** (implicit cast), но **только если это возможно без потери данных и семантически корректно**.
3.  **Если преобразование невозможно — пакет НЕ ЗАПУСТИТСЯ ВООБЩЕ.** Вы получите ошибку на этапе **валидации (validation)**.

---

### **Конкретные сценарии:**

#### **Сценарий A: Environment Variable = `"123"` (string), Parameter = `int`**
*   **Что произойдет:** Движок успешно преобразует строку `"123"` в целое число `123`.
*   **Результат:** Пакет запустится и будет работать. **Это опасный "счастливый случай", который маскирует проблему.**

#### **Сценарий B: Environment Variable = `"123abc"` (string), Parameter = `int`**
*   **Что произойдет:** Преобразование невозможно. Строка содержит нечисловые символы.
*   **Ошибка (примерно такая):**
    ```
    Error: The type of the value being assigned to variable "User::MyParam" differs from the current variable type.
    Variables may not change type during execution. Variable types are strict, except for variables of type Object.
    ```
*   **Результат:** Пакет упадет на этапе валидации, **даже не начав выполнение**.

#### **Сценарий C: Environment Variable = `"123.45"` (string), Parameter = `int`**
*   **Что произойдет:** Движок попытается преобразовать, но поскольку это дробное число, а параметр ожидает целое, может произойти:
    *   **Или** ошибка преобразования (в большинстве случаев)
    *   **Или** усечение до `123` (опасное неявное поведение!)
*   **Результат:** Непредсказуемо, но скорее всего — ошибка.

#### **Сценарий D: Environment Variable = `"2024-01-01"` (string), Parameter = `datetime`**
*   **Что произойдет:** Движок успешно распарсит дату из строки, если формат корректен.
*   **Но:** Формат даты зависит от региональных настроек сервера. `"13/01/2024"` (европейский формат) на сервере с US локалью вызовет ошибку.

---

### **Почему это особенно опасно при обновлении пакета?**

1.  **Тихая поломка:** Вы обновили пакет (изменили тип параметра), развернули его, а **все существующие Jobs перестали запускаться** с неочевидной ошибкой.
2.  **Проблема проявляется только при запуске:** В окне Configure SSMS показывает все красиво (видна привязка), ошибок нет. Проблема всплывает только при попытке выполнения.
3.  **Сложность отладки:** Ошибка происходит на уровне каталога SSIS, до того как начинает выполняться хоть одна задача вашего пакета. В отчетах будет просто "Failed", без деталей из вашего кода.

---

### **Как защититься от этой проблемы?**

#### **1. При разработке (превентивная защита):**
*   **Избегайте изменения типов существующих параметров.** Если нужно изменить тип — создайте новый параметр с другим именем.
*   **Используйте строгую типизацию.** Если параметр должен быть датой — используйте `datetime`, а не `string`.

#### **2. Перед деплоем (проверка):**
```sql
-- Проверить соответствие типов между параметрами и переменными окружения
USE [SSISDB]
SELECT 
    p.object_name AS PackageName,
    p.parameter_name,
    p.data_type AS ParamDataType,
    p.design_default_value AS ParamDefaultValue,
    e.environment_name,
    ev.name AS EnvVarName,
    ev.type AS EnvVarType,
    ev.value AS EnvVarValue,
    -- Флаг несовпадения типов
    CASE 
        WHEN p.data_type != ev.type THEN 'TYPE MISMATCH!'
        ELSE 'OK'
    END AS Status
FROM [catalog].[object_parameters] p
LEFT JOIN [catalog].[environment_variables] ev 
    ON p.referenced_variable_name = ev.name
LEFT JOIN [catalog].[environments] e 
    ON ev.environment_id = e.environment_id
WHERE p.project_name = 'YourProject'
  AND p.value_type = 'R' -- R = Referenced (привязано к Environment)
  AND p.data_type != ev.type;
```

#### **3. В процессе деплоя (автоматическая валидация):**
В вашем скрипте деплоя добавьте проверку:
```powershell
# После деплоя, но ДО применения конфигураций
# Запустить валидацию пакета с текущими Environment
$executionId = $catalog.ExecutePackage("Folder", "Project", "Package.dtsx", $null, @{ "ENVIRONMENT" = "PROD" })
Start-Sleep -Seconds 5 # Дать время на валидацию
$operation = $catalog.Operations[$executionId]
if ($operation.Status -ne 2) { # 2 = Success
    Write-Error "Validation failed after deploy! Check SSISDB reports."
    # Откатить деплой или остановить процесс
}
```

#### **4. После деплоя (ручная проверка):**
*   Всегда делайте **пробный запуск** пакета после деплоя.
*   Не полагайтесь только на то, что "пакет собрался без ошибок".

---

### **Что делать, если изменение типа необходимо?**

1.  **Сценарий с контролем:** 
    *   Измените тип параметра в проекте
    *   **Одновременно** измените тип соответствующих переменных во ВСЕХ Environments (DEV, TEST, PROD)
    *   Только после этого деплойте

2.  **Сценарий без контроля над Environments (самый опасный):**
    *   Создайте **новый параметр** с нужным типом и новым именем (например, `NewCustomerCount_INT`)
    *   В коде пакета добавьте логику преобразования из старого строкового параметра в новый целочисленный (с обработкой ошибок!)
    *   Постепенно перенесите все вызовы на использование нового параметра
    *   Через несколько циклов, когда старый параметр больше не используется, удалите его

---

### **Вопрос на закрепление:**
Представьте, что у вас в Production работает 15 разных SQL Agent Jobs, которые запускают ваш пакет с разными значениями параметров (указанными прямо в свойствах шага Job'а, а не через Environment). Вы изменили тип параметра в пакете. Что произойдет с этими Jobs при деплое новой версии?

**Ответ:** Большинство Jobs **перестанут работать**, так как SSIS Agent Job хранит значения параметров вместе с их **ожидаемым типом**. При изменении типа в пакете возникает несовместимость. Нужно будет вручную обновить настройки параметров в каждом Job'е, что очень трудоемко и рискованно.
